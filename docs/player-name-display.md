# 選手名の表示規則

`players.name_ja` の原典値は変更しない。画面に表示する際だけ、漢字・ひらがなと空白だけで構成される姓名は空白を除いて表示する。

日本語の氏名は一般に姓名を続けて表記するため、`齋藤 直人` と `木田晴斗` をどちらも `齋藤直人` の形式へ揃える。ラテン文字やカタカナを含む名前には適用しない。外国人選手の `Seungsin Lee`、`リーチ マイケル` などでは空白が名前の構造を表すためである。

# Codex 投入バッチ 2026-09-07

`BATCH-2026-09-06-ui.md` の 3 本のうち #773（C5 誤検出）と #774（横スクロール）はマージ済み。本バッチはその後始末 2 件。

| # | 対象 | 仕様 | 触るファイル | 形式 |
|---|---|---|---|---|
| ~~1~~ | ~~会場タイムゾーン~~ | **完了（PR #775 `88a064f`、マージ済み）**。脚注 `/\[[^\]]*\]/g` と辞書 77 表記、510/669 試合で現地行 | | |
| ~~2~~ | ~~監査 summary の可読性~~ | **完了（PR #777、マージ済み）**。82KB → 9.4KB、C1〜C5 不変 | | |

**触るファイルが重ならないので並行可。** ただし 1 は**既存 PR への追加コミット**であって新規 PR ではない。

---

## そのまま貼る文面

以下 2 件を実装してください。触るファイルが重ならないので並行して構いません。

**1 は既存の PR #775 への追加コミットです。新しい PR を作らないでください。** 2 は新規 PR です。

各仕様と指示書は必ず両方を先に全文読んでください。

### 1. 会場タイムゾーンの追い込み（PR #775 への追加コミット）

- 仕様: `specs/fix-venue-local-time-timezone.md`（**2026-09-06 に追記・訂正済み。再読すること**）
- 指示書: `docs/codex-prompts/fix-venue-timezone-followup.md`
- 元データ: `docs/venue-timezone-coverage-2026-09-06.md`

初回実装は正しく、本番プレビューで検証済みです（Townsville が 06:00 BST → 15:00 GMT+10、Twickenham の冬季 GMT 判定も正しい）。直すのは 2 点です。

**(a) 脚注の正規表現。** 仕様に `/\[\d+\]/g` と書いたのが誤りでした。**実装は指示どおりで実装側の欠陥ではありません。** 本番には英字脚注が 18 行 / 12 会場あり（`Eden Park, Auckland[a]`、二重の `…Pukekohe[e][f]` を含む）、これらは辞書に登録しても引けません。`/\[[^\]]*\]/g` に変えてください。

**(b) 辞書のカバレッジ。** 「154 会場の元一覧が無い」と報告していた件は、`docs/venue-timezone-coverage-2026-09-06.md` として用意しました（146 会場 / 669 試合、試合数・大会・ホームチーム付き）。現状の 7 文字列では 669 試合中 32 試合（約 5%）にしか現地行が出ません。**2026-01-01 以降の試合数が 5 以上の会場をすべて登録してください。**

罠が 2 つあります。**`Allianz Stadium` はシドニー・ロンドン・トリノの 3 都市に存在します**（都市名なしの表記もある）。先頭語だけで照合せず、都市名を持たない表記は登録しないでください。もう 1 つ、一覧の `home_teams` 列は**国を判断する材料であって、そこからタイムゾーンを導いてはいけません**。中立会場では複数国が並びます。初回実装で撤去した推定そのものです。

**確信を持てない会場は登録しないでください。** 未登録は現地行が出ないだけで安全側に倒れます。間違った値を入れることが、この仕様が直そうとしているバグそのものです。

略称が `AEST` でなく `GMT+10` で出る件は**対象外**です（仕様の未解決の質問 3 に記録済み）。

### 2. 監査 summary.json の可読性

- 仕様: `specs/fix-audit-summary-collision-payload.md`
- 指示書: `docs/codex-prompts/fix-audit-summary-collision-payload.md`

PR #773 のマージ後、本番実行で C5 は 659 → 0、findings は 678 行 → 87 行になりました。**判定は正しく動いています。**

一方で `summary.json` が 82KB になり読めません。`unreliable_key_collisions` が `match_ids` を全件インラインで持ち、合計 1,385 個・最大 144 個 / エントリになっています。

各エントリを `key` / `value` / `match_count` / `sample_match_ids`（先頭 5 件）に変えてください。**`match_count` が主たる情報**で、`sample_match_ids` は Owner が 1〜2 件開いて確かめるためのものです。先頭 5 件の選び方は決定論であること。

**C1〜C5 の判定と `severity` の式、`findings.csv` には触らないでください。** 出力形式だけです。

`unreliable_key_collisions` は汚染ではありません。`wikipedia_url` がシーズン全試合で共有されるのは Wikipedia の構造上そうなるだけです。**この件数を汚染件数として PR 本文に書かないでください。** 実際の汚染は C1 = 84 / C3 = 8 / C4 = 2 です。

---

## 共通の注意

- **DB への `UPDATE` / `INSERT` / マイグレーションを差分に含めないこと**
- **LLM 呼び出しを増やさないこと。** 2 件ともコスト $0 の決定論処理です
- **本番の env ファイルを使う実行をしないこと。** 本番実行は Owner が行います
- 仕様と現状が食い違う、または受け入れ条件どうしが両立しないと判断したら、**実装を止めて指摘してください。** 今回の (a) は仕様側のバグで、指摘があればもっと早く直せたものです

---

# 追加分（2026-09-06 夜、Owner 判断で解除されたもの）

上記 2 件とは独立。**触るファイルが重ならないので並行可。1 本ずつ別 PR。**

| # | 仕様 | 触るファイル |
|---|---|---|
| 3 | `specs/fix-billing-terms-consistency.md` | `app/legal/tokusho/page.tsx` |
| 4 | `specs/fix-event-ingestion-identity-guard.md` | `lib/ingestion/events.ts`, `scripts/import-world-rugby-full.ts`, `scripts/import-league-one-full.ts`, `lib/llm/notify.ts` |
| 5 | 無料サンプルの差し替え（下記に仕様を直書き） | `lib/sample-matches.ts` |

### 3. 特商法の課金表記（P0）

**Owner が本番 Stripe Checkout で確認しました。トライアルは実在します。**

```
Tryline Premium を試す / 7 日間無料 / その後、¥980/月、2026年9月13日以降
（確認日 2026-09-06 + 7 日 = 09-13 で整合）
```

したがって**料金ページが正しく、特商法が誤り**です。仕様の「未解決の質問」は解消済みなので、そのまま実装できます。直すのは `app/legal/tokusho/page.tsx` の 2 行（支払時期・サービス提供時期）。詳細は仕様に記載。

### 4. 取り込み時の同一性ガード（P1）

**未解決だった 3 件がすべて確定し、着手可能になりました。** 仕様の「未解決の質問」節を再読してください。

- **V4 の識別子**: 許可リスト 5 キー（#773 で確定、`lib/ingestion/external-identifiers.ts` を共有）
- **V3 は警告に留める。取り込みを拒否しない**（Owner 判断 2026-09-06）。本番監査で C3 該当は 848 試合中 8 件で、誤検知の代償の方が大きい
- **独立した 2 実装は本 spec で共通化する**（Owner 判断 2026-09-06）。`scripts/import-world-rugby-full.ts:505` と `scripts/import-league-one-full.ts:335` の独自 `upsertMatchEvents` を削除し共通入口に統一。別 spec に回すとガードに穴が残る

**V4 が守れるのは全 1,372 試合中 327 件（24%）**です。Wikipedia 由来の 1,045 件には fixture 識別子が存在せず、V1〜V3 が主防御になります。**「識別子で同一性を担保した」と完了報告しないでください。**

### 5. 無料サンプル記事の差し替え（P0）

**別途 spec は作りません。以下が仕様です。**

`lib/sample-matches.ts:3` の `PRIMARY_SAMPLE_MATCH_ID` を差し替えます。

```
現在: d31077ee-92c6-480e-bbef-87f955e6bc1d   RWC 2023 決勝 NZ 11-12 南アフリカ
新:   dcd576dd-f778-4690-b4e1-3d960bd664f1   2026-09-05 南アフリカ 29-24 NZ
```

現在のサンプルは 1,295 字で **QA スコアを持ちません**（ルーブリック導入前の生成）。1 点差の試合を「得点力の差が決定づけた」と書いており、監査 A-1 #6 が「サンプルが品質を悪く代表している」と P0 で指摘したものです。

新サンプルは `information_density` 5 / `factual_grounding` 5 / `tactical_depth` 4、**issues 0**、2,577 字。イベント整合の監査でも C1〜C5 のいずれにも該当しません。

**やること**: 定数の差し替えと、`FALLBACK_SAMPLE_MATCH_IDS` の先頭要素の整合。`sample_matches` テーブルは空なのでフォールバック経路が実際に使われています。**DB への INSERT はしないでください。**

**確認**: トップページの無料サンプル枠に新しい試合が出ること。`getPrimarySampleMatchId()` の既存テストがあれば期待値を更新すること。

---

## 本バッチに含めないもの

- `specs/fix-event-ingestion-identity-guard.md` — Owner 判断 2 件が未決（V3 署名一致を自動 reject にするか警告止まりか / 独立した `upsertMatchEvents` 2 実装を直すか）。識別子キー設計は #773 で確定済み。**V4 が守れるのは全 1,372 試合中 327 件（24%）**で、Wikipedia 由来の 1,045 件には fixture 識別子が存在しない。V1〜V3 が主防御である
- `specs/fix-billing-terms-consistency.md` — Stripe 本番 Checkout の無料トライアル有無の確認待ち
- 現地時刻の略称（`GMT+10` → `AEST`）。会場ごとのロケール切替か略称辞書が要り、範囲が変わる
- `"mw-content-text"` を生むパーサの欠陥。#773 の `identifier_quality` で件数が見えるようになったので、**数字を見てから**別 spec で決める
- 監査 A 節のレイアウト・情報設計（情報階層、言語混在、無料サンプル、CTA、ハブの再訪導線）。**測定または Owner のデザイン判断が先**

## Owner 側の残作業

- **#775 の追加コミット後、`f01f68e2` を含む数件でプレビュー実測**（AC15・17・18）
- **2 のマージ後、本番で監査を再実行して `summary.json` が 10KB 未満になったことを確認**（AC13）
- 目視評価: #775 の現地行が消えた試合で JST だけでも不足に見えないか（320 / 768 / 1440px）

`/specs/fix-home-competition-summary-query-fanout.md` の仕様を実装してください。

コンテキスト:
- プロジェクト規約は `AGENTS.md` を読む
- `lib/db/queries/competitions.ts` の `listSeasonsByFamily()` は、呼び出しのたびに `match_content` テーブルから `status='published'` の**全件**（大会・ファミリーで一切絞り込まずサイト全体分）を取得している
- トップページ（`app/page.tsx:149`）は表示対象の大会ファミリーごとにこの関数を並列呼び出ししており、ファミリー数と同じ回数だけ同一の全件取得クエリが繰り返されている

やること:
- `listSeasonsByFamily()` を変更するか、新しい集約関数を用意して、「サイト全体のpublishedコンテンツ全件取得」の重複を解消する。具体的な実装方式（アプリ側で1回だけ取得して計算 vs DB view/RPCで集約）はCodexの裁量とする
- トップページ側の呼び出し方法も、新しい関数・クエリに合わせて変更する

処理すべきエッジケース:
- 各大会ファミリーごとのpublishedコンテンツ件数の計算結果が、変更前後で一致することを確認する
- 表示対象の大会ファミリーが0件・1件・多数件のいずれでも正しく動作すること

完了の定義:
- specs の受け入れ条件1〜4を満たす
- `pnpm test` / `pnpm tsc --noEmit` / `pnpm lint` / `pnpm build` clean
- 変更ファイル一覧を報告する

要件:
- `competitions` テーブルの絞り込みクエリ（`family`で正しく絞られている部分）は変更しない
- ソートロジック（`isRecentlyActive` 等）は変更しない
- 曖昧な箇所や仕様書と実環境の食い違いがあれば、その場で実装を停止して質問する

完了時:
- 実装内容・変更ファイルを要約する
- クエリ回数がどう変化したか（変更前: ファミリー数分、変更後: 何回か）を報告する
- 仕様書からの逸脱があれば理由を明示する
- 未解決の質問があれば記載する

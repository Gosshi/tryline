`/specs/fix-match-detail-page-width.md` の仕様を実装してください。

コンテキスト:
- プロジェクト規約は `AGENTS.md` を読む
- GPT-5.6によるデザイン監査（2026-07-20、1440px・1920px実機確認）で判明: 試合詳細ページ（`app/matches/[id]/page.tsx:485`）のコンテナが `max-w-4xl`（896px）固定で、1920px幅では左右各512pxが余白になっている
- トップページは `fix-home-width-expansion.md`（PR #616、マージ済み）で `max-w-[1536px]` に拡張済みだが、試合詳細ページには波及していない
- `feat-match-page-redesign.md`・`fix-match-detail-page-flow.md` は両方ともマージ済みで、コンポーネント視覚刷新と末尾の「次に見る」導線は対応済み。本specはこの2つが対象外だった「コンテナ幅」だけを扱う

やること:
- `app/matches/[id]/page.tsx:485` の外側コンテナ幅を `max-w-4xl` から `max-w-5xl` 〜 `max-w-6xl`（1152〜1280px目安）へ拡張する
- `components/match-content.tsx`（本文Markdown表示部分）に、外側コンテナより狭い内側カラム（`max-w-2xl` 〜 `max-w-3xl`、720〜800px目安）を適用し、本文の可読性を保つ
- スコアヒーロー・得点グラフ・順位テーブル・ラインアップは拡張後の外側コンテナ幅をそのまま使う（要素の再配置やカラム分割等の構造変更はしない、幅調整のみ）

処理すべきエッジケース:
- 375px幅でオーバーフロー・横スクロールが発生しないこと
- 外側コンテナの正確な幅（`max-w-5xl` / `max-w-6xl` / 任意値）は複数パターンをスクリーンショットで比較提示し、Ownerが最終判断する

完了の定義:
- specの受け入れ条件1〜5を満たす（5番目のOwner目視確認は、実装後にスクリーンショットを添えて報告し、Owner確認を待つ形でよい）
- `pnpm test` / `pnpm tsc --noEmit` / `pnpm lint` / `pnpm build` clean
- 変更ファイル一覧を報告する

要件:
- `feat-match-page-redesign.md` で定義されたコンポーネント構成・デザイントークンは変更しない
- `fix-match-detail-page-flow.md` で実装済みの「次に見る」ブロックの内容は変更しない
- 選手ページ・チームページの幅は対象外（別specで対応）
- 曖昧な箇所や仕様書と実環境の食い違いがあれば、その場で実装を停止して質問する

完了時:
- 実装内容・変更ファイルを要約する
- 1440px・1920px・375pxのスクリーンショットを添付する（外側コンテナ幅の複数パターン比較も含めてよい）
- 仕様書からの逸脱があれば理由を明示する
- 未解決の質問があれば記載する

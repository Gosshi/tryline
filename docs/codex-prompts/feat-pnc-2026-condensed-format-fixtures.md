`/specs/feat-pnc-2026-condensed-format-fixtures.md` の仕様を実装してください。

コンテキスト:
- プロジェクト規約は `CLAUDE.md` を読む
- 対象ファイルは `lib/ingestion/sources/wikipedia-pnc.ts`。現在の `POOL_SECTION_IDS = ["Pool_A", "Pool_B"]`（24行目付近）は、伝統的なプール戦フォーマット前提のセクション探索。2026年は Fiji・Canada・Japan・USA の4か国による凝縮フォーマット（準決勝2試合＋3位決定戦＋決勝）で、Pool A/Bセクション自体が存在しない
- 実際に Wikipedia（`https://en.wikipedia.org/wiki/2026_World_Rugby_Pacific_Nations_Cup`）を確認したところ、準決勝2試合（9/12、Fiji vs Canada・Japan vs United States）は確認できるが、3位決定戦・決勝（ともに9/19）は対戦カード未確定（準決勝の勝者次第）
- この関数は `lib/ingestion/live-competitions.ts` の定期cron（`ingest-live-competitions`）から呼ばれており、呼び出し元の変更は不要

入出力の例:
- Pool セクションが存在しないページ（今年のPNC 2026形式）でも、日付付きの試合情報（準決勝2試合）を正しく抽出できる
- 対戦カード未確定の3位決定戦・決勝は、spec記載の (a)/(b) いずれかの方針で扱う（判断根拠を完了報告に明記）

処理すべきエッジケース:
- Pool セクションが存在する過去シーズン（2022/2024/2025）のパースが壊れないこと（回帰テスト必須）
- 3位決定戦・決勝のチーム名が「Winner SF1」等のプレースホルダー表記の場合の扱い

完了の定義:
- specs の受け入れ条件 1〜5 をすべて満たす
- `pnpm tsc --noEmit` / `pnpm lint` / `pnpm test` / `pnpm build` clean
- Pool セクションが無いページ構造・過去シーズンのPoolセクションありページ構造の両方でユニットテストを追加する

要件:
- スコープ対象外（準決勝2試合の再取り込み、準決勝結果確定後の対戦カード確定処理そのもの）は実装しない
- 未解決の質問（過去シーズンのフォーマット確認、対戦カード未確定期間の扱い方針）について、実装時に確認し、判断に迷う場合は完了報告で質問として提示する。推測しない

完了時:
- 実装内容、変更ファイルを要約する
- 3位決定戦・決勝の扱い（(a)見送り/(b)日程のみ先行登録）のどちらを選んだか、理由とともに明記する
- 仕様書からの逸脱があれば理由を明示する

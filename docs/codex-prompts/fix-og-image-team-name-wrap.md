`/specs/fix-og-image-team-name-wrap.md` の仕様を実装してください。

コンテキスト:
- プロジェクト規約は `CLAUDE.md` を読む
- 対象は `app/api/og/route.tsx` の `type=result` 分岐（169行目以降）、home/away チーム名描画部分（262-298行目付近）。現在 `fontSize: "58px"` 固定で、長いチーム名（「ニュージーランド」8文字等）が文字境界で折り返される
- `@vercel/og`（Satori）で描画されるため、通常のCSSの `white-space`/`text-overflow` は使えない。フォントサイズを名前の文字数に応じて動的に変える方針で対応する
- Owner提供のスクリーンショットで実際の不具合（「ニュージーラン」「ド」に分断）を確認済み

入出力の例:
- `home="ニュージーランド"`（8文字）の場合、フォントサイズを縮小し1行に収める
- `home="日本"`（2文字）の場合、現状通り58pxの大きなフォントサイズを維持する

処理すべきエッジケース:
- 想定されるチーム名の最大文字数（日本語表記済みのチーム名一覧を確認し、最長のものでも1行に収まるしきい値を設定する）
- スコア表示（中央の `resultScore` div、`minWidth: 240px`）とのバランスが崩れないこと（チーム名を縮小しすぎて不自然に小さくならないこと）

完了の定義:
- specs の受け入れ条件 1〜5 をすべて満たす
- `pnpm tsc --noEmit` / `pnpm lint` / `pnpm test` / `pnpm build` clean
- 文字数としきい値のユニットテストを追加する
- 実際に `/api/og?type=result&home=ニュージーランド&away=イタリア&competition=...` 相当のパラメータで画像を生成し、目視確認した結果を完了報告に含める

要件:
- `type=competition` 等、他の画像タイプは変更しない
- 曖昧なしきい値設定は自身の判断で決めてよいが、根拠（実際のチーム名一覧の最長文字数等）を完了報告に明記する

完了時:
- 実装内容、選んだフォントサイズしきい値を要約する
- 目視確認した画像の結果を報告する
- 仕様書からの逸脱があれば理由を明示する

`/specs/fix-competition-hub-width.md` の仕様を実装してください。

コンテキスト:
- プロジェクト規約は `AGENTS.md` を読む
- 大会ハブページ（`app/c/[competition]/page.tsx`）の主要コンテンツコンテナは `max-w-4xl`（896px、134行目）で、1440px画面では左右合計約544pxの余白になっている。カード一覧を表示するには狭い

やること:
- `app/c/[competition]/page.tsx:134` の `max-w-4xl` を `max-w-6xl`（1152px）に拡張する
- ヒーロー部分のテキストオーバーレイ（121行目の `max-w-4xl`）はそのまま維持してよい
- 長文になりうる大会ガイド本文がある場合は、そのブロックだけ内側で `max-w-3xl` 程度に制限する

処理すべきエッジケース:
- モバイル幅（375px前後）の表示に影響を与えないこと
- ヒーローテキストの可読性が変更前と同等であること

完了の定義:
- specs の受け入れ条件1〜5を満たす（5番目のOwner目視確認は、実装後にスクリーンショットを添えて報告し、Owner確認を待つ形でよい）
- `pnpm test` / `pnpm tsc --noEmit` / `pnpm lint` / `pnpm build` clean
- 変更ファイル一覧を報告する

要件:
- 2カラム化（「最新／ガイド」と「レビュー／アーカイブ」）は本specの必須要件ではない。幅拡張のみで一旦区切ってよい（spec本文「未解決の質問」参照）
- 曖昧な箇所や仕様書と実環境の食い違いがあれば、その場で実装を停止して質問する

完了時:
- 実装内容・変更ファイルを要約する
- デスクトップ1440px幅のスクリーンショットを添付する
- 仕様書からの逸脱があれば理由を明示する
- 未解決の質問があれば記載する

`/specs/feat-style-guard-shadow-mode.md` の仕様を実装してください。

コンテキスト:
- プロジェクト規約は `AGENTS.md` を読む
- AI文体（定型句・過去記事との類似・japanese_quality）の判定を、公開判定に一切影響させず、計測のみ行うのが本specの目的
- 既存のretry機構（`lib/llm/stages/generate-narrative.ts`）は具体的な失敗理由を次の生成試行にフィードバックしていないため、厳格なゲートをいきなり導入するとretry/reject率が急増するリスクがある。そのため段階導入の第一段階として、まず影響を計測する

やること:
- 3種類の判定ロジックを実装する（`japanese_quality`が3のケースのフラグ記録、AI文体定型句検出、直近記事との類似度チェック）
- 判定結果を記録する仕組みを用意する（`match_content.qa_scores` への追記、または新規記録先。既存スキーマとの整合性を見て判断する）
- 直近50〜100件のpublished記事に対して一括で判定をかけ直すバッチスクリプトを実装する

処理すべきエッジケース:
- 既存の `resolveVerdict()` の判定結果には一切影響を与えないこと（既存テストが全て通ること）
- 比較対象の既存記事が少ない大会・シーズンでも動作すること

完了の定義:
- specs の受け入れ条件1〜5を満たす
- `pnpm test` / `pnpm tsc --noEmit` / `pnpm lint` clean
- 変更ファイル一覧を報告する
- バッチスクリプトの実行結果（集計レポート）を報告する

要件:
- 追加のLLM呼び出しは発生させない
- `resolveVerdict()` や既存の公開判定フローを変更しない
- 曖昧な箇所や仕様書と実環境の食い違いがあれば、その場で実装を停止して質問する

完了時:
- 実装内容・変更ファイルを要約する
- バッチスクリプトの集計結果（新ルールなら直近N件中何件がretry相当になるか）を具体的な数値で報告する
- 仕様書からの逸脱があれば理由を明示する
- 未解決の質問があれば記載する

`/specs/fix-home-hero-heading-widow-line.md` の仕様を実装してください。

コンテキスト:
- プロジェクト規約は `CLAUDE.md` を読む
- 対象は `app/page.tsx` の249-261行目、h1内の `<wbr/>` 配置。「今週の」[wbr]「海外」[wbr]「ラグビー」[wbr]「を、」[br]「日本時間」[wbr]「で追う。」という構成のうち、「ラグビー」の直後（「を、」の直前）の `<wbr/>` を削除する
- 本番ページで実際にこの `<wbr/>` を削除して検証済み: 幅1440px前後で「今週の海外」「ラグビーを、」「日本時間で追う。」の自然な3行になることを確認している
- この見出しは過去に `fix-home-hero-heading-overflow.md`（PR #501）で320px幅でのオーバーフロー対策として `<wbr/>` が追加された経緯がある。今回の変更でその対策を後退させないこと

入出力の例:
- 変更前: `今週の<wbr/>海外<wbr/>ラグビー<wbr/>を、<br .../>日本時間<wbr/>で追う。`
- 変更後: `今週の<wbr/>海外<wbr/>ラグビーを、<br .../>日本時間<wbr/>で追う。`（「ラグビー」と「を、」の間の`<wbr/>`のみ削除）

処理すべきエッジケース:
- 320px・375px幅で、PR #501が対応したオーバーフロー（画面端からのはみ出し）が再発しないことを必ず確認する。スクリーンショットまたはDOM計測（`getBoundingClientRect`等）で検証する
- 768px・1024px・1440pxの主要ブレークポイントでも見出しの改行が不自然でないことを確認する

完了の定義:
- specs の受け入れ条件 1〜4 をすべて満たす
- `pnpm tsc --noEmit` / `pnpm lint` / `pnpm test` / `pnpm build` clean
- 320/375/768/1024/1440の各幅でのスクリーンショットまたはDOM計測結果を完了報告に含める

要件:
- 他の `<wbr/>`（「今週の」後・「海外」後・「日本時間」後）は変更しない
- 曖昧な箇所があれば末尾に質問として列挙する

完了時:
- 実装内容を要約する
- 各ブレークポイントでの検証結果（オーバーフロー無し・改行が自然であること）を報告する
- 仕様書からの逸脱があれば理由を明示する

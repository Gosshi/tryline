`/specs/feat-paper-texture-background.md` の仕様を実装してください。

コンテキスト:
- プロジェクト規約は `CLAUDE.md` を読む
- 基準ビジュアルは `docs/design/mock-bg-a-paper.html` を参照する
- 対象は `app/globals.css`(`body` の背景定義)と、`bg-slate-50` を `<main>` に直書きしている15ファイル(spec本文に列挙済み)
- 現状、ページ本体の背景(`--color-paper` / `bg-slate-50`)とカード(`bg-white`)がほぼ同じ明るさで、境界線と影だけに頼っている。今回はカードでなく**土台(ページ背景)側**に暖色グラデーション + 微細グレインを足して、既存の白カードとのコントラストを作る

入出力の例:
- 変更前: `<main className="min-h-screen bg-slate-50">`(フラットなほぼ白の背景)
- 変更後: `<main className="min-h-screen bg-paper">`(暖色グラデーション + グレイン。`bg-paper` は `app/globals.css` に新規定義する共有クラス)

処理すべきエッジケース:
- グレインテクスチャ(SVG `feTurbulence` によるノイズ)の不透明度が強すぎて本文テキストの可読性を損なわないこと。仕様書のSVG例をベースに、透過率が十分低い(ほぼ気づかない程度)ことを確認する
- `background-attachment: fixed` は使わない(モバイルSafariでの既知の不具合を避けるため)。デフォルトの `scroll` のままでよい
- 15ファイル全てで `bg-slate-50` → `bg-paper` の置き換えが漏れなく行われていること(`grep -rn "bg-slate-50" app` で置き換え後に0件になることを確認)
- `app/globals.css` の `--color-paper` を参照している箇所が他にもあれば、影響を確認し完了報告に記載する

完了の定義:
- specs の受け入れ条件7項目すべてを満たす(スコープ3の任意対応は実装してもしなくてもよいが、見送る場合は理由を明記する)
- `pnpm tsc --noEmit` / `pnpm lint` / `pnpm build` clean
- ホーム・試合詳細・カレンダー・料金ページの320/768/1024/1440pxスクリーンショットを提示する

要件:
- 「対象外」にある項目(案Bのダーク実装、トグル機能、カード自体の色変更)は実装しない
- 曖昧な箇所があれば末尾に質問として列挙する。推測しない

完了時:
- 実装内容、変更ファイルを要約する
- スコープ3(アンビエントカラー)を実装したか見送ったか、理由とともに明記する
- 仕様書からの逸脱があれば理由を明示する
- Owner への未解決の質問があれば記載する

`/specs/feat-upcoming-fixture-visual-redesign.md` の仕様を実装してください。

コンテキスト:
- プロジェクト規約は `AGENTS.md` を読む
- トップページ「今後の試合」セクション（`app/page.tsx:366-421`）は `TeamBadge`（`components/team-badge.tsx`）を `size={18}` の小さいサイズで表示しており、「次に見るべき試合」が視覚的に判別しにくい
- `TeamBadge` は既に国旗SVG・チームカラーグラデーションを描画できる汎用コンポーネントで、サイズ調整のみで対応できる想定

やること:
- 「今後の試合」リストの先頭1件（最も近いキックオフ）を「注目の次戦」として大きく扱う: `TeamBadge` を40〜48px程度で表示し、キックオフ時刻を強調し、チームカラーを左右の細いレール（stripe）として使う
- 2試合目以降はコンパクトな行として28〜32px程度のバッジで表示する
- `design.md` の既存方針（team colorはstripe/top bar/低濃度gradientとして補助的に使う）の範囲内で実装する

処理すべきエッジケース:
- 表示対象の試合が1件のみの場合、2件目以降のコンパクト表示は当然空になる
- モバイル幅（375px前後）でレイアウト崩れ・横スクロールが起きないこと

完了の定義:
- specs の受け入れ条件1〜5を満たす（5番目のOwner目視確認は、実装後にスクリーンショットを添えて報告し、Owner確認を待つ形でよい）
- `pnpm test` / `pnpm tsc --noEmit` / `pnpm lint` / `pnpm build` clean
- 変更ファイル一覧を報告する

要件:
- `TeamBadge` コンポーネント自体の内部実装（flag/gradient描画ロジック）は変更しない。サイズ調整とレイアウト変更のみで対応する
- 実在ロゴ・公式ユニフォームへ寄せた表現は使わない
- 表示件数（現状5件）は変更しない
- 曖昧な箇所や仕様書と実環境の食い違いがあれば、その場で実装を停止して質問する

完了時:
- 実装内容・変更ファイルを要約する
- デスクトップ・モバイルのスクリーンショットを添付する
- 仕様書からの逸脱があれば理由を明示する
- 未解決の質問があれば記載する

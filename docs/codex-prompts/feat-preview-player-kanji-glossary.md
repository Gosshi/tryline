`specs/feat-preview-player-kanji-glossary.md` の仕様を実装してください。

**着手前に必ず読むこと**: `specs/feat-japanese-player-kanji-names.md`（マージ済み、PR #600）。本 spec は同 spec が `match_events` に限定していたスコープを、チームのロスターへ広げるものです。既存の挙動を壊さないでください。

コンテキスト:
- プロジェクト規約は `AGENTS.md` を読む
- 過去の判断は `docs/decisions.md` を読む
- **調査済みの事実（再調査不要）**:
  - `japanese_name_glossary` は `assemble.ts:933-962` で組み立てられ、957行で `loadedMatchEvents.japanesePlayerNameGlossary` のみを取り込んでいる
  - `generate-preview.ts:198-205` と `generate-recap.ts:279-286` は**既にグロッサリを読んでいる**。プロンプト側の変更は不要
  - `assemble.ts:356` のラインナップ取得は `name_ja` を select していない
  - **予定試合の `match_lineups` は0件**（2026-08-08 実測）。ラインナップ経路だけを直してもプレビューには効かない
  - 日本代表81名のうち `players.name_ja` が入っているのは11名のみ
  - 既存ヘルパー `buildJapanesePlayerNameGlossary` が `assemble.ts:461` にある
- 変更対象:
  - `lib/llm/stages/assemble.ts`（ロスター取得の追加、ラインナップ select の修正、グロッサリのマージ）
  - 必要なら `scripts/backfill-japan-player-kanji-names.ts`

実装のポイント:
- **本 spec の中心は「ロスター由来のグロッサリ」です。** 試合の出場者に限定せず、対戦2チームに所属する `players` のうち `name_ja` が非 null の行からグロッサリを作ってください。これによりラインナップやイベントの有無に依存しなくなります
- **`buildJapanesePlayerNameGlossary` を再利用すること。** 同等の関数を新設しないでください
- イベント由来・ラインナップ由来・ロスター由来をマージし、**重複排除**してください
- `assemble.ts:356` の select に `name_ja` を追加してください（レビューで得点していない選手を拾うため）

**2026-08-08 の再生成で判明した追加の不具合（本 spec の範囲で調査してください）**:

QA 修正後にプレビューを再生成したところ、**全選手が「姓・名」順のカタカナで出力されました**。日本人選手だけでなく外国籍選手にも適用されており、明確な誤りです。

| 生成結果 | 正しい表記 |
|---|---|
| イシダ・キッペイ | 石田 吉平 |
| サイトウ・ナオト | 齋藤 直人 |
| **ライリー・ディラン** | ディラン・ライリー |
| **ディアーンズ・ワーナー** | ワーナー・ディアンズ |
| **レイチ・マイケル** | リーチ マイケル |
| **ロス・エイダン** | エイダン・ロス |

グロッサリを直しても、この姓名逆転は解決しません。`nameStyleInstruction`（`generate-preview.ts` / `generate-recap.ts`）の日本人向け姓名順ルールが全選手へ適用されている可能性があります。**該当箇所を特定し、原因と修正案を報告してください。** 修正が本 spec のスコープを超えると判断した場合は、実装せず報告だけで構いません（別 spec に切り出します）。

エッジケース:
- `match_events` と `match_lineups` がいずれも0件の予定試合でグロッサリが空にならないこと（**最重要**）
- グロッサリに載っていない外国籍選手の姓名順が壊れないこと
- `name_ja` が非 null の選手が0名のチーム（海外チームの大半）でクエリ結果が空になっても既存挙動に影響しないこと
- `name_ja` が null の選手はグロッサリに入れないこと（外国籍選手のカタカナ表記を壊さない）
- 同一選手が複数経路に現れたときの重複
- ロスターが大きいチームでもプロンプトが破綻しないこと

やらないこと:
- **`generate-preview.ts` / `generate-recap.ts` のプロンプト変更**。グロッサリを読む仕組みは実装済みです
- 日本代表以外への `name_ja` データ投入（仕組みは汎用にしてよいが、データは日本代表のみ）
- `players` のスキーマ変更
- 姓名の順序・区切り文字の変更。**DB の `name_ja` をそのまま使う**（現状「石田 吉平」形式）
- 既存 published 記事の再生成
- `scripts/backfill-japan-player-kanji-names.ts` の dry-run 既定と `--confirm-owner-approved` の変更

テスト:
- **`match_events` と `match_lineups` が0件でもロスター由来でグロッサリが構築されること**（本 spec の中心的なテスト）
- ラインナップ由来のエントリが入ること
- 3経路で同一選手が重複しないこと
- `name_ja` が null の選手が入らないこと
- 既存のイベント由来グロッサリのテストが引き続き通ること

完了の定義:
- spec の受け入れ条件1〜10をすべて満たす
- `pnpm lint` / `pnpm tsc --noEmit` / `pnpm test` / `pnpm build` clean
- 変更ファイル一覧を報告する

完了時:
- 実装内容を要約する
- **ロスター取得をどのクエリで行ったか、N+1 になっていないかを報告してください**
- グロッサリの重複排除をどう実装したかを報告する
- 日本代表81名のうち11名しか `name_ja` が無い現状で、生成結果がどう変わるかの見込みを述べてください
- 曖昧な箇所や仕様書と実環境の食い違いがあれば、その場で実装を停止して質問する

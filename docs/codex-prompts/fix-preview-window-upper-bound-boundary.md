仕様書 `specs/fix-preview-window-upper-bound-boundary.md` を実装してください。**先に全文を読んでください。**

GPT-6 再レビュー 2026-09-11 の **F3** です。**実装はあなたの逸脱ではありません。仕様の境界定義が誤っています。**

## 何が起きるか

`lib/cron/preview-window.ts:29-30`。

```ts
// その 15:00 が担当する JST キックオフ日は「翌日」。その日の終わり = +33 時間
return new Date(lastReleaseJst + 33 * HOUR_MS - JST_OFFSET_MS).toISOString();
```

15:00 + 33h = **翌々日 00:00 JST**。doc コメント（`:6-7`）は「**この値以下**の kickoff_at」とし、呼び出し 3 経路とも `lte` です。

```
lib/cron/orchestrate.ts:221 → :97                        .lte("kickoff_at", ...)
app/api/cron/audit-prekickoff-readiness/route.ts:42
app/api/cron/matches-with-late-lineups/route.ts:52       .lte("match.kickoff_at", ...)
```

**「翌日の終わり」を翌々日 00:00 と表現して inclusive で比較しているため、翌々日 00:00 ちょうどのキックオフが窓に入ります。**

```
9/11 15:00 実行  →  上限 9/13 00:00 かつ inclusive
9/13 00:00 キックオフ  →  本来の期限は 9/12 15:00 なのに 9/11 15:00 に候補になる（24時間早い）
```

D030 の「キックオフ前日 15:00」より 24 時間早く初回生成されえます。

## やること

**返り値と比較演算子をセットで直してください。片方だけでは同じ穴が残ります。**

| 方針 | 返り値 | 比較 |
|---|---|---|
| A | 翌日 23:59:59.999 JST | `lte` のまま（呼び出し元 3 経路とも無変更） |
| B | 翌々日 00:00 JST | 3 経路とも `lt` へ |

**A のほうが変更が小さい**です。どちらでも構いませんが、**3 経路すべてで揃えてください。** doc コメント（`:6-7`）も返り値の意味と一致させてください。

**採用した方針と、3 経路で揃っていることを PR 本文に書いてください。**

`specs/feat-preview-generation-day-before-1500.md` の境界定義と検算表も、**訂正の経緯つきで**直してください。「なぜ 33 時間だったか」「なぜ誤りか」を残します。次に読んだ人が 33 時間に戻さないためです。

## やってはいけないこと

- **JST 15:00 という公開時刻を変えること**（D030）
- cron のスケジュールを変えること
- 生成パイプライン・LLM 呼び出しを触ること
- 既に早く生成されたコンテンツを修復すること

## 完了の定義

受け入れ条件 1〜11 を満たすこと。特に:

- **翌々日 00:00 ちょうどが窓に入らない**（条件 1）
- **その 1 ミリ秒前は窓に入る**（条件 2）
- **翌日 00:00 ちょうどは窓に入る**＝下端が動いていない証拠（条件 3）
- 実行時刻 15:00 / 14:59:59.999 / 15:00:00.001 それぞれで上限が期待どおり（条件 4）
- **3 経路で返り値と比較演算子の組が揃っている**（条件 5）

**テストは日付を固定してください**（`vi.setSystemTime`）。実行日に依存するテストにしないでください — 2026-09-11 に `tests/app/team-page.test.tsx` が同じ理由で CI を落としました。置き場所は `exclude` 非該当を確認してください。

git worktree で `origin/main` から切ってください（`docs/runbooks/codex-worktree.md`）。

**該当時刻の試合が現在登録されているかは未確認です。** 本番での発生件数は測っていません。「生成タイミングを直した」と報告する際、実害があったかは別問題として扱ってください。

仕様と現状が食い違うと判断したら、実装を止めて指摘してください。

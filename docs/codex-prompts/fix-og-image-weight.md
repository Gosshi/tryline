`/specs/fix-og-image-weight.md` の仕様を実装してください。

コンテキスト:
- プロジェクト規約は `CLAUDE.md` を読む
- 対象は `public/og-image.png`（1枚のアセット差し替え）のみ
- `sharp`（`package.json` に既存、`^0.34.5`）を使って圧縮する。新規依存の追加は不要

入出力の例:
- 変更前: `public/og-image.png` が約1.4MB（1200x630）
- 変更後: 同じパス・同じ次元（1200x630）で 300KB 以下

処理すべきエッジケース:
- 次元（1200x630）を変えないこと。次元が変わると OG カードのレイアウトが崩れる
- 拡張子・フォーマットは PNG のまま維持すること（JPEG/WebP に変えると参照先7ファイルのコード変更が必要になりスコープ外になる）
- 圧縮に使った一時スクリプトはコミットせず実行後に削除すること

完了の定義:
- `public/og-image.png` が300KB以下、次元1200x630のまま
- 圧縮前後で視覚的な劣化が目視でわかるレベルでないことをスクリーンショットか画像プレビューで確認する
- `og-image.png` を参照する7ファイルに一切コード変更が無い（`git diff` で `public/og-image.png` 以外に差分が無いことを確認）
- `pnpm build` が通る

要件:
- 受け入れ条件セクションのすべてを実装する
- 300KB に届かない場合は quality をどこまで下げたか、最終的なファイルサイズを完了報告に明記する
- 曖昧な箇所があれば末尾に質問として列挙する。推測しない

完了時:
- 実装内容（圧縮前後のファイルサイズ）を要約する
- 仕様書からの逸脱があれば理由を明示する
- Owner への未解決の質問があれば記載する

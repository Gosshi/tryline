# Codex 投入バッチ 2026-09-05

12本の spec の投入順序。**この順序は GPT-6 のレビュー（`docs/audits/gpt6-spec-review-and-skill-update-2026-09-05/review.md` A-0）の衝突分析に基づく。**

| 便 | 本数 | 並行可否 | 状態 |
|---|---:|---|---|
| 第1便 | 6 | **並行可**（触るファイルが完全に非重複） | 投入可 |
| 第2便 | 1 | 単独 | 第1便と並行可 |
| 第3便 | 5 | **直列**（共通の純関数を共有） | 第2便の完了後 |
| 保留 | 1 | — | Owner の Stripe 確認待ち |

---

## 第1便（そのまま貼る）

以下 6 本を実装してください。**触るファイルが重ならないので、並行して進めて構いません。1本ずつ別 PR にしてください。**

各仕様と指示書は必ず両方を先に全文読んでください。指示書に書いた「やってはいけないこと」は、過去に実際に起きた事故に基づいています。

| # | 仕様 | 指示書 | 触るファイル |
|---|---|---|---|
| 1 | `specs/fix-h2h-latest-vs-next-match.md` | `docs/codex-prompts/fix-h2h-latest-vs-next-match.md` | `app/h2h/[pair]/page.tsx` |
| 2 | `specs/fix-competition-guide-participant-verification.md` | `docs/codex-prompts/fix-competition-guide-participant-verification.md` | `tools/audit-competition-guide-facts.ts`（新規） |
| 3 | `specs/fix-health-endpoint-status-and-llm-monitoring.md` | `docs/codex-prompts/fix-health-endpoint-status-and-llm-monitoring.md` | `app/api/health/route.ts` |
| 4 | `specs/fix-stripe-webhook-db-error-propagation.md` | `docs/codex-prompts/fix-stripe-webhook-db-error-propagation.md` | `app/api/stripe/webhook/route.ts`, `app/api/revenuecat/webhook/route.ts` |
| 5 | `specs/fix-rwc2027-match-count-and-broadcast-claims.md` | `docs/codex-prompts/fix-rwc2027-match-count-and-broadcast-claims.md` | `app/c/rwc/2027/page.tsx` |
| 6 | `specs/fix-season-faq-broadcast-hardcoding.md` | `docs/codex-prompts/fix-season-faq-broadcast-hardcoding.md` | `app/c/[competition]/[season]/page.tsx` |

**5 と 6 だけ注意**: どちらかが `components/competition-viewing-guide.tsx` を変更する必要が出た場合は、**その2本だけ直列にしてください**（同じファイルを両方が触ると衝突します）。ページ内で完結するなら並行で構いません。

### 6本すべてに共通する条件

**`pnpm test` が green であることを完了根拠にしないでください。** `vitest.config.ts:16` の `exclude` が `tests/health.test.ts` `tests/db/**` `tests/ingestion/events.test.ts` `tests/llm/pipeline.test.ts` `tests/llm/stages/assemble.test.ts` を実行しません。

各仕様の「テスト実行の条件」に従い、**実際に実行したコマンドと結果を PR 本文に貼ってください。**

**本番の設定・データを変更しないでください。** Stripe の価格や Webhook 登録、Supabase のデータには触れません。コード差分だけです。

**`sk_live_` から始まる本番キーを扱わないでください。**

### 3（health）の補足

仕様の「未解決の質問」に Owner 未確定の項目が 2 つあります。**`status` の集約（`checks` から `ok` / `degraded` / `error` を導出し、`error` で 503 を返す）だけを先に実装して構いません。** `checks.generation` の細部が決まるまで待つ必要はありません。

---

## 第2便（第1便と並行可・単独で投げる）

**イベント整合の判定を、共有できる純関数として切り出してください。これだけを1つの PR にしてください。**

後続 5 本がすべてこの関数に依存します。**先にこれを入れないと、同じ判定が 5 回書かれます。**

### 作るもの

`lib/ingestion/event-integrity.ts`（新規）に、DB クライアントに依存しない純関数として次を置いてください。

- イベント配列と最終スコアから、整合しているかを判定する関数
- `MatchEventRow`（`lib/db/queries/match-events.ts`）から判定に必要な形へ変換する関数

### 守ること

**得点換算を書き起こさないでください。** `lib/format/match-event-points.ts` の `pointsForMatchEvent` を使ってください。これが換算の唯一の正で、`assemble.ts:159` `live-ingest.ts:241` `player-stats.ts:76` が既に使っています。2026-06 に `penalty_try` を 5 点として扱う誤りが実在しました。penalty try は独立した type ではなく `type="try"` に `metadata.is_penalty_try` が立つ形です。

**`lib/data-integrity/audit.ts:117` の `toScoreEvent` は export されていません。** import できないので、変換は新しく定義してください。

**DB クライアントを import しないでください。** この関数は React コンポーネントからも呼びます。`lib/llm/stages/assemble.ts` を経由させないでください。

**`match_events` に `player_name` 列はありません。** `metadata: Json` の中（`metadata.player_name`）です。

### 触ってよいファイル

```
lib/ingestion/event-integrity.ts        （新規）
lib/llm/stages/assemble.ts              （既存判定を新関数へ委譲。挙動は変えない）
lib/data-integrity/audit.ts             （import 先の変更のみ）
```

**この PR で挙動を変えないでください。** 既存のテストがそのまま通ることが条件です。

---

## 第3便（第2便のマージ後・記載の順に直列）

**この 5 本は同じ純関数と同じ判定結果を共有するため、順番に入れてください。**

| 順 | 仕様 | 指示書 | 理由 |
|---|---|---|---|
| 1 | `specs/fix-contaminated-events-display-isolation.md` | 同名 | 読者に出ている誤ったグラフ・一覧を最初に止める |
| 2 | `specs/feat-published-content-integrity-disclosure.md` | 同名 | 1 の判定を本文の開示にも使う |
| 3 | `specs/fix-generation-event-integrity-gate.md` | 同名 | 新しい記事の生成を止める |
| 4 | `specs/audit-published-recap-event-integrity.md` | 同名 | 汚染範囲を数える読み取り専用ツール |
| 5 | `specs/fix-event-ingestion-identity-guard.md` | 同名 | 入口のガード本体（最も複雑・未解決事項あり） |

**5 は「未解決の質問」に Owner が決める項目が 3 つあります。着手前に Owner に確認してください。**

---

## 保留（投げないでください）

`specs/fix-billing-terms-consistency.md` は、**Owner が「Stripe の本番 Product / Price に無料トライアルが設定されているか」を確認するまで着手できません。** どちらに揃えるかで直す対象のページが変わります。

仕様書 `specs/feat-published-content-integrity-disclosure.md` を実装してください。**先に全文を読んでください。**

## 実装前に確認すること

**`specs/fix-contaminated-events-display-isolation.md` は実装・マージ済みですか。**

そちらが整合判定の純関数と `event_integrity` を用意します。**先にそちらを入れてください。** 逆順だと同じ判定を2回書くことになります。

**未実装なら、実装せずに Owner に確認してください。**

## 何を作るか

**既に公開されている誤った本文が、読者に出続けています。**

`/matches/f01f68e2-bdd6-47c8-8910-0ea37a382b0a` の recap は 2026-08-17 公開で、今も読めます。イベント表示を止めても本文の「5分、ハリー・ホッキングスのトライで0–5と先制」は残ります。

先行する4本の spec は、入口・生成・イベント表示・棚卸しを扱いますが、**本文には誰も触れません。** その穴を埋めます。

## 触るファイル

```
components/match-content-trust-strip.tsx
app/matches/[id]/page.tsx   （4箇所の props）
```

**新しいバナーやモーダルを作らないでください。** 既に「この記事の根拠」を出している trust strip に、状態を1つ足すだけです。

## 文面で書いてはいけないこと

**「この記事は誤っています」と書かないでください。** 生成時点のデータは正しかった可能性があり、現在のデータとの照合結果でしかありません。

**「AI が生成したため」と書かないでください。** 原因はデータの取り込みであって生成モデルではありません。

既定の文面は「この記事の得点経過は現在の記録と一致していません。確認のうえ更新します。」です。

## 混同してはいけない2つの仕様

`specs/fix-regen-reject-preserves-published.md` は **一時的な失敗で本文を失わない**ための仕様で、`lib/llm/pipeline.ts:769-770` にあります。

**本 spec が扱うのは逆の判断**で、誤りだと分かっている本文を意図的に隔離することです。**同じ仕組みに混ぜないでください。** 温存ロジックは変更しません。

## 撤回機構を新設しないでください

**既存の `match_content.status` を使います。** `lib/db/queries/match-content.ts` は L78 / L111 / L178 で `.eq("status","published")` により絞っているので、`draft` にすればページ・API・RSS のすべてから消えます。

**新しいフラグ列を足さないでください。マイグレーションを作らないでください。**

## 絶対に守ること

**記事を自動で unpublish しないでください。** 誤検知で正しい記事を消します。判断は Owner です。

**コード差分に `match_events` / `match_content` への DELETE / UPDATE を作らないでください。** 撤回は Owner の運用であって、コードの機能ではありません。

**本文を LLM に読ませて正誤を判定しないでください。**

## デザイン

`--color-accent-subtle` の丸バッジは「ラインアップ確認済み」など**肯定的な根拠**に使われています。確認中の表示にこれを使わないでください。区別できる中立の見た目にしてください。

Owner の目視評価が受け入れ条件に入っています（320 / 768 / 1440px）。

## テスト

**`pnpm test` だけを完了根拠にしないでください。** `vitest.config.ts:16` の `exclude` が `tests/db/**` 等を実行しません。

DB をモックした除外外の新規ファイルに置くか、除外を外した実行コマンドを用意して、**実際に実行した結果を PR 本文に貼ってください。**

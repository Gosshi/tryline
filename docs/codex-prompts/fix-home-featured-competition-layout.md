`/specs/fix-home-featured-competition-layout.md` の仕様を実装してください。

コンテキスト:
- プロジェクト規約は `CLAUDE.md` を読む
- この仕様は事前調査（`docs/codex-prompts/research-home-featured-competition-whitespace.md`）の結果を踏まえたもの。調査結果の推奨案（横長バナー化）をそのまま実装する
- `app/page.tsx`の「注目大会」セクション（350行目付近）と、「次戦」計算ロジック（177行目付近、今週最大6試合のプールからの抽出）を確認すること
- `components/home-matchday-board.tsx`（104行目付近の表示上限ロジック）には触れない

入出力の例:
- PNC 2026の次の試合が2026-09-12にある場合、「次戦」欄に正しくその日時・対戦カードが表示される
- デスクトップ1440px幅で「注目大会」セクション全体の高さが約220〜260pxに収まる

処理すべきエッジケース:
- 該当大会に今後の試合が1件も無い場合のフォールバック表示（「未定」ではなく、シーズン終了・オフシーズン等が分かる自然な文言にする）
- モバイル表示（375px）は現状問題ないため、大きく変更せず縦積みレイアウトを維持する

完了の定義:
- specs の受け入れ条件 1〜5 をすべて満たす（受け入れ条件6のスクリーンショット確認は自分で実施し、本番デプロイのみOwnerが別途行う）
- `pnpm tsc --noEmit` / `pnpm lint` / `pnpm test` / `pnpm build` clean
- Playwright等で実際に1440px・375px双方の表示を確認したスクリーンショットを完了報告に含める

要件:
- スコープ対象外（HomeMatchdayBoard自体の変更、順位表/レビュー一覧の追加、大会自動選択の仕組み）は実装しない
- ビジュアルデザインの詳細に迷う場合は完了報告で質問として提示する

完了時:
- 実装内容、変更ファイルを要約する
- 「次戦」ロジックの修正内容とテスト結果を報告する
- 仕様書からの逸脱があれば理由を明示する

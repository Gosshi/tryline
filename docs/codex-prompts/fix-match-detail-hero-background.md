`/specs/fix-match-detail-hero-background.md` の仕様を実装してください。

コンテキスト:
- プロジェクト規約は `CLAUDE.md` を読む
- 対象は `components/match-header.tsx:64-77` のヒーロー `<section>` の `style` プロパティ内 `background` 値
- `public/visuals/match-detail-bg.jpg` は既に生成済みでリポジトリに配置されている（新規画像生成は不要、このファイルをそのまま使う）
- CSSの複数背景レイヤーはカンマ区切りで前方が手前・後方が奥。既存の4層グラデーション（暗幕オーバーレイ→ホーム色ラジアル→アウェイ色ラジアル→対角グラデーション）の最後尾に画像URLレイヤーを追加する

入出力の例:
- 変更前: ヒーロー背景はチームカラーから生成した4層グラデーションのみ
- 変更後: 同じ4層グラデーションの下に `url(/visuals/match-detail-bg.jpg)` が追加され、`backgroundSize`/`backgroundPosition` をレイヤー数分（5層）指定する

処理すべきエッジケース:
- 既存のグラデーション層は画像より手前に描画されるため、チームカラーの可読性（白文字の見出し・スコア・バッジ）に影響が出ないことを実際の試合ページ数件で確認する
- `backgroundSize`/`backgroundPosition` の値はカンマ区切りの数がレイヤー数（5）と一致していること
- 大会・チームカラーの組み合わせが変わっても画像とグラデーションの重なりが崩れないこと

完了の定義:
- specs の受け入れ条件5項目すべてを満たす
- `pnpm tsc --noEmit` / `pnpm lint` / `pnpm build` clean
- 変更前後のスクリーンショット、または実装箇所のdiffを提示する

要件:
- 受け入れ条件セクションのすべてを実装する
- 「対象外」にある項目（大会別バリエーション、新規画像生成）は実装しない
- 曖昧な箇所があれば末尾に質問として列挙する。推測しない

完了時:
- 実装内容、変更ファイルを要約する
- 仕様書からの逸脱があれば理由を明示する
- Owner への未解決の質問があれば記載する

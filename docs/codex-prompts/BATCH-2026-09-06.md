# Codex 投入バッチ 2026-09-06

前回バッチ（`BATCH-2026-09-05.md`）の第1便6本・共有純関数・表示隔離・本文開示はすべてマージ済み（#759-767）。本バッチはその続き。

| 便 | 本数 | 並行可否 |
|---|---:|---|
| 第1便 | 4 | **並行可**（触るファイルが非重複） |
| 第2便 | 2 | 直列（第1便の一部と同じファイル） |
| 保留 | 2 | Owner の判断待ち |

---

## 第1便（そのまま貼る・並行可）

以下 4 本を実装してください。**触るファイルが重ならないので並行して構いません。1本ずつ別 PR にしてください。**

各仕様と指示書は必ず両方を先に全文読んでください。指示書の「やってはいけないこと」は過去に実際に起きた事故に基づいています。

| # | 仕様 | 触るファイル |
|---|---|---|
| 1 | `specs/fix-competition-display-name-duplication.md` | `lib/db/queries/match-content.ts` |
| 2 | `specs/fix-data-integrity-alert-actionability.md` | `lib/llm/notify.ts`, `lib/data-integrity/audit.ts` |
| ~~3~~ | ~~`specs/fix-stats-exclude-unverified-events.md`~~ | **不要（2026-09-06）。#770 で目的が満たされた。実装しないこと** |
| 4 | `specs/fix-generation-event-integrity-gate.md` | `lib/llm/pipeline.ts`, `lib/llm/stages/assemble.ts`, `lib/llm/notify.ts` |

**2 と 4 は直列**: どちらも `lib/llm/notify.ts` を触ります。2（通知の作り直し）を先に入れ、4 はその形に合わせてください。

**3 は取り下げました**（2026-09-06）。4（#770）が不整合時に `match_events` を空にした結果、QA の `hasEvents` が false になり、選手別統計の照合が既に走らなくなっています。追加実装は不要です。

### 4本すべてに共通する条件

**`pnpm test` が green であることを完了根拠にしないでください。** `vitest.config.ts` の `exclude` が `tests/llm/pipeline.test.ts` / `tests/llm/stages/assemble.test.ts` / `tests/db/**` / `tests/health.test.ts` / `tests/ingestion/events.test.ts` を実行しません。**あなたが書くテストの多くがこの領域に入ります。**

除外されていない新規ファイルに置くか、除外を外した実行コマンドを用意し、**実行したコマンドと結果を PR 本文に貼ってください。** #767 は `tests/llm/pipeline-published-preservation.test.ts` という別名にして除外を回避しました。同じ手が使えます。

**本番のデータ・設定を変更しないでください。** コード差分だけです。マイグレーションを作らないでください。

**判定ロジックを書き起こさないでください。** イベント整合の判定は `lib/ingestion/event-integrity.ts` にあります（#765 で切り出し済み、DB 非依存）。得点換算は `lib/format/match-event-points.ts` の `pointsForMatchEvent` が唯一の正です。

**`PROMPT_VERSION` をバンプするのは 3 だけです**（2026-09-06 訂正）。**4 はプロンプト文字列を変更しないのでバンプしません。** 初版の記述が誤りで、仕様書（`fix-generation-event-integrity-gate.md` の対象外と AC9）が正です。

**既存の公開記事を再生成しないでください。** 再生成の範囲は Owner が別途決めます。2026-06 に 297 件が draft 化して本番から消えた事故があります。

---

## 第2便（第1便のマージ後）

| 順 | 仕様 | 理由 |
|---|---|---|
| 1 | `specs/fix-recap-narrative-ignores-supplied-stats.md` | 3 と同じ `qa-content.ts` を触る |
| 2 | `specs/audit-published-recap-event-integrity.md` | 4 のマージ後に、汚染範囲を数える読み取り専用ツール |

---

## 保留（投げないでください）

**`specs/fix-event-ingestion-identity-guard.md`** — 「未解決の質問」に Owner が決める項目が 3 つあります（V4 の fixture 識別子キー設計、V3 を自動拒否に使うか、共通入口を通らない独立実装2つの扱い）。着手前に Owner の回答が必要です。

**`specs/fix-billing-terms-consistency.md`** — Owner が「Stripe の本番 Product / Price に無料トライアルが設定されているか」を確認するまで、どちらのページを直すか決まりません。

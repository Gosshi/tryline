# Codex プロンプト: feat-mobile-editorial-polish

**tryline-mobile リポジトリ**で貼る（仕様書コピー設置済み: `docs/specs/feat-mobile-editorial-polish.md`）。**束1（fix-mobile-audit-bugs）のマージ後**に着手（同じファイルを触るため直列）。

---

`docs/specs/feat-mobile-editorial-polish.md` の仕様を実装してください。デザイン監査（2026-07-15）の両モデル一致診断「白カード均質で Web のエディトリアルと乖離」への対応で、**見た目のみ**の変更です。

コンテキスト:
- `AGENTS.md` を読む
- 方針は仕様書冒頭の一文がすべて:「角丸と影を減らし、罫線と密度で紙面をつくる。色・フォント・情報は変えない」
- トークン変更（radius・Card の影→罫線）を先にやり、コンポーネント個別対応（ヘッダー統合・カード内カード解消・密度・状態・eyebrow 削除・Switch 色）を後に積む
- 挙動追加は仕様書の 6b（0試合シーズン末尾送り）・7（空状態の action）・9b（マスク案内タップで開示）の 3 点のみ。それ以外で既存テストが落ちたら、変更が見た目を越えていないか疑うこと

エッジケース:
- eyebrow 削除は詳細/モーダル画面のみ。タブ直下 3 画面（今週・大会・設定）の大見出しは残す
- 赤い上罫線は 2pt。太くしない（アクセントは最小限、が方針）
- 密度圧縮でタップターゲットを 44pt 未満にしない（束1 で直した箇所を再悪化させない）

完了の定義:
- 受け入れ条件 1〜12 のテスト、CI green
- **before/after 比較スクリーンショット**（監査時の 01 / 04 / 05 / 07 / 08 / 09 / 10 / 12 / 13 相当）を PR に添付 — 受け入れ条件 13 の Owner 目視判定の材料
- 実装内容・変更ファイルの要約、仕様書からの逸脱（あれば理由）を報告

仕様書 `specs/fix-ai-label-product-naming.md` を実装してください。**先に全文を読んでください。**

**置換だけでは失敗です。開示の追加とセットで行ってください。**

## 何が問題か

D（`project_ai_labeling`）で「UI 文言から AI を外す」と決めましたが、後日対応が未着手で **13 箇所**残っています。表記も揺れています（`AI チャット` / `AIチャット` / `AI CHAT`）。

```
app/pricing/page.tsx:38,50,80,153,270,294,315,319
components/match-chat.tsx:287        AI CHAT
components/sample-recap-cta.tsx:46
lib/billing/terms.ts:37,41,52        ← 2026-09-09 追加。あなたの指摘で判明
```

**指摘は正しかったです。** 初版は `app/` と `components/` に絞り、さらに `--include='*.tsx'` を付けて grep した結果を「10 箇所」と書いていました。`lib/billing/terms.ts` は `lib/` 配下の `.ts` で、両方の条件で除外されます。止めてもらって助かりました。

### `lib/billing/terms.ts` の 3 箇所が特に重要な理由

リテラルの場所と表示される場所が違います。

| 定数 | 出力先 |
|---|---|
| `pricingDescription`（`:37` / `:52`） | `app/pricing/page.tsx:21` の `metadata.description` と `:23` |
| `trialFaqAnswer`（`:41`） | `app/pricing/page.tsx:45` の FAQ 回答 → **FAQPage JSON-LD** |

**検索結果に出る説明文と構造化データに残ります。** 画面本文だけ置換すると、最も露出する場所が直りません。

**`:37` と `:52` は `trialDays > 0` と `trialDays === 0` の別分岐です。** 片方だけ直すと、トライアル設定を変えた瞬間に表記が食い違います。

## 最重要の制約

監査の原文です。

> 商品名は「この試合について質問する」へ揃え、回答がAI生成で根拠/限界があることは利用箇所に明示する。**「AI」を消すことを透明性の削減にしない。**

**現状、チャット画面に「回答が生成物であり限界がある」旨の明示はありません。**

`components/match-chat.tsx:21,25` に「記録された得点経過をもとに」「公開済みのレビューと試合データをもとに」という文言はありますが、**これは回答範囲の説明であって、生成物であることの開示ではありません。これを開示として流用しないでください。**

つまり **「AI」を機械的に置換すると、透明性が現状より下がります。**

## やること

1. 商品名を統一する。監査の推奨は「**この試合について質問する**」。表記揺れも同時に解消する
2. **チャットの利用箇所に、回答が生成物であることの開示を追加する**

D の「敵は AI でなく間違い」に照らすと、開示は「AI だから間違うかも」ではなく、**何をもとに答えており、何は答えられないか**であるべきです。

## やってはいけないこと

- **チャット機能のロジック（`components/match-chat.tsx` の処理、`lib/chat/`）を変えること**
- **プロンプトテンプレートを変えること**
- 価格・プラン構成を変えること
- **`lib/billing/terms.ts` の課金条件を変えること。** `trialDays` / 価格 / `paymentTiming` / `serviceProvisionTiming` は触らないでください。同ファイルは `f44ec12` で**課金文言の単一の権威**になったもので、特商法表記と料金ページの整合がここに依存しています。**触ってよいのは「AI チャット」という商品名の部分だけ**です
- `app/pricing/page.tsx:84` の `uploadDate` を触ること。**同時に投げている `fix-pricing-video-upload-date` が扱います**
  - ※ `:80` の `description` は**こちらが触ります**。`VideoObject` の他のプロパティは壊さないでください
- レビュー・プレビュー本文中の表現を変えること
- **`tryline-mobile` を変えること。** web と同時に変えると検証が分散します

## 完了の定義

受け入れ条件 1〜11 を満たすこと。特に:

- **チャットの利用箇所に生成物であることの開示がある**（条件 3）
- **開示に、何をもとに答えているかの範囲が含まれる**（条件 4）
- **`components/match-chat.tsx:21,25` を開示として流用していない**（条件 5）
- **`lib/chat/` に差分が無い**（条件 7）
- `tryline-mobile` に差分が無い（条件 10）
- **`metadata.description` と FAQPage JSON-LD に「AI チャット」が残っていない**（条件 6-a・6-b）
- **`terms.ts:37` と `:52` の両分岐が同じ商品名**（条件 6-c）
- **課金条件に差分が無い**（条件 6-d）
- 既存の `tests/app/pricing-page.test.tsx` が green（条件 6-e。必要なら期待値を更新）

商品名の案と開示の文言を PR 本文に書いてください。

テストは `tests/app/` と `tests/components/` 配下へ（`exclude` 非該当。確認済み）。

git worktree で `origin/main` から切ってください（`docs/runbooks/codex-worktree.md`）。

**モバイル側は変わりません。** web とモバイルで商品名が一時的に食い違います。「AI 表記を全部直した」と報告しないでください。

仕様と現状が食い違うと判断したら、実装を止めて指摘してください。

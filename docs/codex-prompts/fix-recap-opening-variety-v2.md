`/specs/fix-recap-opening-variety-v2.md` の仕様を実装してください。

コンテキスト:
- プロジェクト規約は `CLAUDE.md` を読む
- 対象は `lib/llm/prompts/shared-prompt-blocks.ts` の `PROHIBITIONS_BLOCK`（18-32行目、既存の「得点力」関連禁止句が29行目にある）と `lib/llm/prompts/generate-recap.ts`
- 前回の同種修正 `fix-recap-opening-variety.md`（実装済み）が禁止句リスト方式を採用した経緯と、今回それが別フレーズへの乗り換えで再発した背景を理解した上で着手する

入出力の例:
- 現状: 「47対38というスコアが示すように、試合はスコットランドの攻撃力が光った一戦となった。」のような冒頭が繰り返し生成されている（直近15件中8件、53%）
- 修正後の期待: 冒頭が「22分のシューマンによるトライで試合の流れが決まった。」のような具体的瞬間、または試合前予想との対比、大会文脈のいずれかから始まる

処理すべきエッジケース:
- `PROHIBITIONS_BLOCK` への追加（応急処置）と、構造レベルの指示追加（恒久対策）の両方を実装すること。片方だけでは不十分
- `dataSparseBlock`（データが乏しい試合向けの分岐）と、通常ケースの両方に構造指示が及ぶようにする（既存の分岐構造を確認してから追加箇所を決める）
- `PROMPT_VERSION` の更新を忘れない（既存の類似specでの慣例に従う）

完了の定義:
- specs の受け入れ条件 1〜6 をすべて満たす
- `pnpm tsc --noEmit` / `pnpm lint` / `pnpm test` / `pnpm build` clean
- 本 spec は再生成を伴わない。既存公開済みrecapへの遡及適用はしない

要件:
- 「スコープ対象外」（generate-preview.ts、見出し構造自体、QAプロンプトへの反映）は実装しない
- 曖昧な箇所があれば末尾に質問として列挙する。推測しない

完了時:
- 実装内容、変更ファイルを要約する
- 仕様書からの逸脱があれば理由を明示する
- Owner への未解決の質問があれば記載する

`/specs/fix-home-rwc2027-link.md` の仕様を実装してください。

コンテキスト:
- プロジェクト規約は `CLAUDE.md` を読む
- 対象ファイルは `app/page.tsx` のみ
- `homepageCompetitionLinks` のデータソース（`lib/db/queries/competitions.ts` の `selectLatestSeasonWithMatches`）は変更しないこと。これは他の全大会の表示に使われる共通ロジックで、RWC のためだけに変更すると他大会が壊れる

入出力の例:
- 変更前: ホームの大会アーカイブに「Rugby World Cup / 2023 最新シーズン→」のカードのみ表示され、`/c/rwc/2027` への導線が無い
- 変更後: 同じカードに加えて、`/c/rwc/2027` を指す小さな補助リンク（例:「2027年大会（オーストラリア開催）の日程はこちら →」）が表示される

処理すべきエッジケース:
- この補助リンクは `family === "rwc"` の場合のみ表示すること。他の大会には一切表示しない
- 既存の RWC カード本体（`/c/rwc/2023` へのリンク）は変更しないこと
- モバイル幅（375px）でリンクのタップ領域が窮屈にならないか確認すること

完了の定義:
- `app/page.tsx` に `/c/rwc/2027` への `<Link>` が追加されている
- ホームページの HTML に `href="/c/rwc/2027"` が含まれることを確認する（ローカルで `pnpm dev` 起動後 `curl localhost:3000 | grep 'rwc/2027'` 等）
- 他大会のカード表示に差分が無いことを確認する
- `pnpm tsc --noEmit` / `pnpm build` が通る

要件:
- 受け入れ条件セクションのすべてを実装する
- spec の「代替案」（カード内サブリンク vs 独立バナー）はどちらを選んでもよいが、選んだ理由を完了報告に書く
- 曖昧な箇所があれば末尾に質問として列挙する。推測しない

完了時:
- 実装内容、変更ファイルを要約する
- 仕様書からの逸脱があれば理由を明示する
- Owner への未解決の質問があれば記載する

`specs/fix-broadcast-service-mapping.md` の仕様を実装してください。**明日（2026-08-15）14:15 の日本代表戦に間に合わせたい変更です。**

コンテキスト:
- プロジェクト規約は `AGENTS.md` を読む
- 過去の判断は `docs/decisions.md` を読む
- 背景: 2026-08-14 の `ingest-broadcasts` cron が「未対応サービス: 日本テレビ系全国ネット / WOWOWライブ / WOWOWオンデマンド」を報告し、**8/15 の日本代表 vs オーストラリア戦に放送情報が1件も入っていない**
- **取り込み経路は正常に動いている。** 同じ実行で 9/05 日本×カナダの3件は投入できている。原因は対応表の不足だけ

参考にする既存パターン:
- **対応表**: `lib/broadcasts/ingest.ts:73` の `KIND_BY_SERVICE_NAME`。現在7サービスのみ
- **未対応の扱い**: 同ファイル 276〜282行。`KIND_BY_SERVICE_NAME[serviceName]` が undefined なら `unknownServices` に積む
- **保存**: 同ファイル 209〜217行。`upsert(rows, { onConflict: "match_id,service_name" })`
- **スクレイパー**: `lib/scrapers/jrfu-match-broadcasts.ts`（取得元は `https://www.rugby-japan.jp/schedule/`）。**変更しない**

追加するサービス（**2026-08-14 に JRFU 公式日程ページで実際に確認した表記のみ**）:

| サービス名 | kind |
|---|---|
| `WOWOWライブ` | `tv` |
| `WOWOWオンデマンド` | `streaming` |
| `日本テレビ系全国ネット` | `tv` |
| `WOWOWプライム` | `tv` |

エッジケース:
- **推測でサービスを足さない。** 上記4つ以外を勝手に追加しないこと。実データで確認できたものだけ
- **`日本テレビ系全国ネット` は 8/15 の試合とは無関係。** 9/05 カナダ戦の地上波中継である。8/15 に必要なのは WOWOW の2つだけ
- **サービス名を正規化してから対応表を引く。** 既存データに `J SPORTS オンデマンド` と `J SPORTSオンデマンド`、`WOWOW オンデマンド` と `WOWOWオンデマンド` が併存しており、**空白1つで未対応になる**。前後の空白除去に加え、全角・半角スペースを除去して比較する
- **保存する `service_name` は正規化後の値にする。** 元表記のまま保存すると重複が増える
- **未対応サービスの報告は残す。** `unknownServices` に積んで cron 通知に出す現在の設計は正しい。誤った kind を推測で割り当てるより安全
- **1件が未対応でも、同じ試合の対応済みサービスは保存されること。** 現在の挙動を確認し、全滅する作りなら報告すること

やらないこと:
- 取得元の追加（Wikipedia 等。spec の未解決の質問1）
- `lib/scrapers/jrfu-match-broadcasts.ts` の変更
- 既存 `match_broadcasts` の表記ゆれデータのクリーンアップ
- 放送情報の UI 変更（実装済み）
- `docs/decisions.md` / `specs/*.md` / `CLAUDE.md` / `AGENTS.md` の変更

完了の定義:
- spec の受け入れ条件1〜7をすべて満たす
- テストを追加する。最低限、次の4ケース
  1. `WOWOWライブ` が `tv` として解決される
  2. **`J SPORTS オンデマンド`（空白あり）が `J SPORTSオンデマンド` として解決される**
  3. 保存される `service_name` が正規化後の値になっている
  4. 対応表に無いサービスは `unknownServices` に積まれ、**同じ試合の対応済みサービスは保存される**
- `pnpm lint` / `pnpm tsc --noEmit` / `pnpm test` / `pnpm build` clean
- 変更ファイル一覧を報告する

完了時:
- 実装内容を要約する
- **正規化の実装方針を説明する**（どの文字を除去し、どう比較するか）
- spec の受け入れ条件を1項目ずつ、満たしたことをどう確認したかと合わせて報告する（「CI green」だけの報告は不可）
- **1件未対応のときに他のサービスが保存されるかどうか**、現在の挙動を明記する
- 曖昧な箇所や仕様書と実環境の食い違いがあれば、その場で実装を停止して質問する

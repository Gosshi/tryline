仕様書 `specs/fix-rwc2027-match-count-and-broadcast-claims.md` を実装してください。**先に全文を読んでください。**

## 何を直すか

**RWC 2027 ハブが、同じページの中で矛盾しています。**

`app/c/rwc/2027/page.tsx:84-90` の `PreTournamentBanner` は「全{matchCount}試合のスケジュールが確定しています」と出しますが、`matchCount` は **DB に登録済みの件数（現在36）**です。同じページの大会ガイドには「24チーム・52試合」と書いてあります。

**36 はプール戦の数です。大会全体は 52 試合です。** 収録件数を大会全体の確定件数に一般化しています。

さらに同じページの視聴方法が **2027年の放送を断定しています**。「J SPORTS : 全試合を生中継」など。**RWC 2027 の日本国内放送権は未発表です。**

## 触るファイル

```
app/c/rwc/2027/page.tsx
```

## やってはいけないこと

**`matchCount` を 52 にするために試合データをバックフィルしないでください。** 収録件数は収録件数のまま表示します。直すのは「収録件数を大会全体として書いている」ことです。

**大会概要ガイドを LLM で再生成しないでください。** ガイドの「24チーム・52試合」は正しい記述です。

## 必ず入れるテスト

**`matchCount` を 52 に変えても「全52試合のスケジュールが確定しています」と出力しないこと。** 将来 52 件を収録したときに、同じ誤りが復活しないようにします。

## デザイン

既存の `rounded-lg border border-[var(--color-rule)] bg-white px-6 py-4 text-sm` を維持してください。**警告色を使わないでください。** 事実を正しく書くだけです。

## ガイド本文には触らないでください

`competition_guides`（`family='rwc'`）の `guide_ja` に「### 日本での視聴方法 / DAZN : … / J SPORTS : 全試合を生中継 / …」というプロセスが入っており、`CompetitionViewingGuide` が markdown として描画します。

**これは Owner が DB を直します。Codex は触らないでください。** `guide_ja` の書き換え、マイグレーション、markdown のフィルタ処理、いずれも作らないでください。

**あなたの担当はページのコードだけです。** ページ側にサービス名の断定を書かないこと、それだけが受け入れ条件です。

**マージのタイミングに注意してください。** Owner がガイドを直す前にこの PR がマージされると、新セクションの「未発表です」の直下にガイドの「全試合を生中継」が並び、同じ見出しが2つ矛盾した内容で出ます。**マージは Owner がガイドを直した後です。**

`specs/feat-standings-standalone-pages.md` の仕様を実装してください。

**着手前に必ず読むこと**:
- `app/c/[competition]/[season]/page.tsx` — 順位表が現在どう描画されているか、日本語の大会名をどう解決しているか
- `app/c/[competition]/[season]/round/` — 既存の下位ルーティング。URL 設計をここに揃えます
- `specs/p1-competition-standings.md`（マージ済み）— 順位表データ層の初期実装。**取り込みロジックには触れません**

コンテキスト:
- プロジェクト規約は `AGENTS.md` を読む
- 過去の判断は `docs/decisions.md` を読む
- **調査済みの事実（再調査不要。2026-08-10 実測）**:
  - GSC（2026-07-10〜08-06）で「順位」を含むクエリが**78インプレッション**。全体約300の4分の1
  - 最大は「シックス ネーションズ 順位」53インプレッション・平均順位8.4・**クリック0**
  - 順位表は現在**大会ハブ内の一要素**で、専用 URL が無い
  - `StandingsTable` コンポーネントは既存で、トップ・試合詳細・大会ハブの3箇所で使用中
  - `competition_standings` は12大会以上にデータあり（RWC 2027=24行、URC 2025-26=16行、Top 14 2025-26=14行、NC 2026=12行、SRP 2026=11行、Premiership 2025-26=10行、Six Nations 各年=6行）
  - **`competitions.name_ja` が null でも日本語表示される経路が別に存在する**（2026-08-10 確認）。大会ハブの実装を読んで同じ経路を使うこと
- 変更対象:
  - `app/c/[competition]/[season]/` 配下に新規ページ
  - 大会ハブへの導線追加
  - サイトマップ生成

実装のポイント:
- **`StandingsTable` を変更しないこと。** 既存の描画をそのまま流用します
- **大会ハブ内の順位表表示は残すこと。** 独立ページへ移すのではなく、両方に置きます（回遊が途切れないように）
- **URL は既存のルーティング規則に揃えること。** `round/` と同じ階層の付け方にしてください。決めた形を報告すること
- タイトル・説明は**順位表を主題に**すること。大会ハブの「最新結果・次戦・日程・順位」のような要素の羅列にしないでください
- **最終更新日時を表示すること。** 順位表は鮮度が価値で、いつ時点かが分からないと信頼できません

エッジケース:
- **順位表が存在しない大会（ツアー形式）で空のページを生成しないこと。** `greatest-rivalry-2026`・`lipovitan-challenge-cup-2026` 等が該当します。404 かリダイレクトかを判断し、報告してください
- 順位表が1行しかない大会
- 過去シーズン（Six Nations は2020年から）
- 同一大会で複数シーズンが存在する場合

やらないこと:
- **`StandingsTable` の変更**
- **順位表データの取り込み・更新ロジックの変更**（`p1-competition-standings.md` 等の結果を維持）
- 大会ハブからの順位表削除
- 順位の計算・並び替えロジックの変更
- 新しい構造化データ語彙の発明。既存ページの手法に合わせること

テスト:
- 順位表を持つ大会で 200 が返ること
- **順位表が無い大会で空ページが生成されないこと**
- 大会ハブ内の順位表表示が維持されていること
- 大会ハブから独立ページへの導線があること
- サイトマップに含まれること

完了の定義:
- spec の受け入れ条件1〜10をすべて満たす
- `pnpm lint` / `pnpm tsc --noEmit` / `pnpm test` / `pnpm build` clean
- 変更ファイル一覧を報告する

完了時:
- 実装内容を要約する
- **採用した URL の形と、既存ルーティングとの整合の根拠を報告してください**
- **過去シーズンをどこまで生成するか、その判断根拠を報告してください。** 2026-08-02 に index bloat（無意味な選手ページ）を noindex 化した経緯があり、薄いページの量産は避ける必要があります
- 順位表が無い大会の扱い（404 / リダイレクト）と根拠を報告してください
- 日本語の大会名をどの経路で解決したかを示してください
- 曖昧な箇所や仕様書と実環境の食い違いがあれば、その場で実装を停止して質問する

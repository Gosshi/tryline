`/specs/feat-bento-card-redesign.md` の仕様のうち、**「UI サーフェス」節の4のみ**を実装してください（料金ページFAQのヒーロー＋アコーディオン化）。1・2（ホームページ）・3（カレンダー共有コンポーネント）は別PRで対応するため、このPRでは触らないでください。

コンテキスト:
- プロジェクト規約は `CLAUDE.md` を読む
- 基準ビジュアルは `docs/design/mock-cards-b-bento.html`（`.faq-hero` / `.faq-list` セクション）を参照する
- 対象は `app/pricing/page.tsx:320-339`（FAQセクション）と `faqs` 配列（`app/pricing/page.tsx:38-65`、変更不要・参照のみ）
- 新規クライアントコンポーネント `components/pricing-faq.tsx` を作成し、`"use client"` で開閉状態を管理する。`app/pricing/page.tsx` 自体はサーバーコンポーネントのまま、`faqs` 配列を props で渡す

入出力の例:
- 変更前: `faqs` 全6件が同じ `rounded-xl border` カードで、Q+Aとも常時表示
- 変更後: `faqs[0]`（「無料トライアルはありますか？」）は常時展開のヒーロー表示（Q+A表示）。`faqs[1]`以降はアコーディオン形式で、初期状態は質問のみ表示、クリックで回答が展開/折りたたみされる

処理すべきエッジケース:
- キーボード操作（Tab でフォーカス、Enter/Space で開閉）に対応すること。`<button>` 要素をトリガーに使う、または `aria-expanded` を適切に設定する
- 複数のアコーディオン項目を同時に開けるようにするか、1つだけ開く（他は自動的に閉じる）かは実装の裁量でよいが、末尾の完了報告でどちらにしたか明記すること
- SSR/CSR境界（`"use client"` の切り出し）で `pricingVideoJsonLd` 等、`app/pricing/page.tsx` の他のJSON-LD構造化データに影響が出ないこと

完了の定義:
- specs の受け入れ条件のうち4・6・7・8を満たす（1・2・3は対象外）
- `pnpm tsc --noEmit` / `pnpm lint` / `pnpm build` clean
- 展開前・展開後それぞれのスクリーンショットを提示する

要件:
- `faqs` 配列の内容（質問・回答テキスト）自体は変更しない
- 曖昧な箇所（同時に複数開けるか等）があれば末尾に質問として列挙する。推測しない

完了時:
- 実装内容、変更ファイルを要約する
- アコーディオンの開閉挙動（単一開閉か複数可か）を明記する
- 仕様書からの逸脱があれば理由を明示する
- Owner への未解決の質問があれば記載する

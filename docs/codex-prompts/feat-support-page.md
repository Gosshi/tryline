`specs/feat-support-page.md` の仕様を実装してください。

コンテキスト:
- プロジェクト規約は `AGENTS.md` を読む
- 過去の判断は `docs/decisions.md` を読む
- 背景: 2026-08-06 の App Store 審査で Guideline 1.5 によりリジェクトされた。Support URL に特定商取引法のページを指定していたため。サポート専用ページが必要
- 参考にする既存パターン:
  - ページのレイアウト・文字組み・見出し階層: `app/legal/privacy/page.tsx`（同じ構成を踏襲する）
  - 連絡先の表記と `mailto:` リンクの書き方: `app/legal/privacy/page.tsx:150-157`
  - 返信目安の文言: `app/legal/tokusho/page.tsx:19-20`（`support@trylinerugby.com`、3営業日以内）。**数字と表現をここに揃える**
  - `generateMetadata` と canonical の書き方: 既存の法務ページに準拠
- 新規ファイルは `app/support/page.tsx` の1枚。加えてサイト共通フッターにリンクを追加する

エッジケース:
- フッターは複数のレイアウトから使われている可能性がある。共通コンポーネントを1箇所直せば全ページに反映されることを確認する
- 既存フッターに法務リンクの並びがある場合はその並びに加える。無ければ既存の見た目を壊さない位置に置く

やらないこと:
- 問い合わせフォームの実装。メール受付のままにする
- `app/legal/` 配下3ページの変更
- 英語版ページの作成
- `robots` を noindex にすること（既定のままでよい）
- Premium 解約手順の iOS 側を断定的に書くこと。IAP は別 spec で実装中なので、iOS については「App Store のサブスクリプション設定から」という一般的な表現に留める

完了の定義:
- spec の受け入れ条件1〜7をすべて満たす
- `pnpm lint` / `pnpm tsc --noEmit` / `pnpm test` / `pnpm build` clean
- 変更ファイル一覧を報告する

完了時:
- 実装内容を要約する
- ローカルで `/support` を開いたスクリーンショットを添えて報告する
- フッターのどのコンポーネントを変更したか、それが全ページに反映されることをどう確認したかを報告する
- 曖昧な箇所や仕様書と実環境の食い違いがあれば、その場で実装を停止して質問する

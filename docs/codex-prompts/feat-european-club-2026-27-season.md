`specs/feat-european-club-2026-27-season.md` の仕様を実装してください。

**着手前に必ず読むこと**: `specs/feat-six-nations-2027-live-source.md`（マージ済み）。シーズン追加の前例です。

コンテキスト:
- プロジェクト規約は `AGENTS.md` を読む
- 過去の判断は `docs/decisions.md` を読む
- **調査済みの事実（再調査不要。2026-08-09 に本番実測）**:
  - Premiership 2025-26 は75試合・全て `finished`（2025-09-25〜2026-06-20）
  - URC 2025-26 は150試合・全て `finished`（2025-09-26〜2026-05-16）
  - Top 14 2025-26 は5試合・全て `finished`（2026-06-13〜2026-06-27）
  - **2026-27 シーズンは3大会とも1試合も存在しない**
  - `buildWikipediaUrl(season)` は引数を取る設計だが、`fetchPremiership202526()`（172-173行）等が `"2025-26"` を直書きしている。URC は163行、Top 14 は187行に同じ構造
  - `live-competitions.ts` 90-112行の登録も `competitionName` / `competitionSlug` / `season` がすべて固定値
  - **`https://en.wikipedia.org/wiki/2026–27_Premiership_Rugby` は既に存在し、全18ラウンドの日程が掲載済み**（開幕 2026年9月25日、決勝 2027年6月19日）
  - **同ページは未実施の試合を `0-0` として表示している**
- 変更対象:
  - `lib/ingestion/sources/wikipedia-premiership.ts`
  - `lib/ingestion/sources/wikipedia-urc.ts`
  - `lib/ingestion/sources/wikipedia-top-14.ts`
  - `lib/ingestion/live-competitions.ts`
  - `competitions` への行追加（マイグレーション不要、データ投入は Owner が実施）

実装のポイント:
- **最重要: 未実施試合の `0-0` をスコアとして取り込まないこと。** Wikipedia の 2026-27 ページは未実施試合を `0-0` と表示します。そのまま取り込むと「0-0で終わった試合」が18ラウンド分生成されます。**既存パーサがこれをどう扱うかを実装前に確認し、必要なら分岐を入れてください。テストで必ず担保すること**
- 取得関数をシーズン引数化する。`LiveCompetitionSource.fetch` は引数を取らないため、登録側でクロージャを使う（`fetch: () => fetchPremiership("2026-27")`）
- 既存の `fetchPremiership202526` 等の扱いは判断してよい。呼び出し元が `live-competitions.ts` のみなら置き換えて構いません
- 2025-26 のエントリを巡回対象から外すかは判断してよい。**外す場合、過去シーズンのハブページが DB のデータで引き続き表示されることを確認**し、判断と根拠を報告してください

エッジケース:
- **未実施試合が `status='finished'` にならないこと**（最重要）
- **未実施試合のスコアが `0`（`null` でなく）にならないこと**
- Wikipedia ページが存在しないシーズンで空配列を返すこと（`isMissingWikipediaPage` の既存挙動）
- 2025-26 の既存データ（75/150/5件）が削除・変更されないこと
- 昇降格で `teams` に未登録のクラブがある可能性。**未登録があれば報告してください**（チーム追加は本 spec の対象外）

やらないこと:
- **Top 14 のパーサ改修**。2024-25 が6試合、2025-26 が5試合しか取れておらず、6月のプレーオフのみで正規シーズン182試合が丸ごと欠落しています。既知の別課題であり、本 spec では扱いません
- 各パーサの解析ロジック変更（`parsePremiershipLiveHtml` 等）
- 2025-26 シーズンのデータ削除
- 大会ハブページ・順位表 UI の変更
- `teams` への新規クラブ登録
- 他大会のシーズン繰り上げ

テスト:
- **未実施試合が `finished` にならず、スコアが `0-0` にならないこと**（最重要）
- 取得関数がシーズン引数を受け取り、正しい URL を組み立てること
- `live-competitions.ts` に 2026-27 の3エントリが登録されていること
- ページ不在時に空配列を返すこと
- 既存の他大会の取り込みに影響がないこと

完了の定義:
- spec の受け入れ条件1〜8をすべて満たす
- `pnpm lint` / `pnpm tsc --noEmit` / `pnpm test` / `pnpm build` clean
- 変更ファイル一覧を報告する

完了時:
- 実装内容を要約する
- **既存パーサが未実施試合の `0-0` をどう扱っていたか、対応が必要だったかを報告してください**
- **URC・Top 14 の 2026-27 Wikipedia ページが存在したかを報告してください**
- 2025-26 を巡回対象から外したか、その判断根拠を報告してください
- 昇降格で未登録のクラブがあれば一覧を報告してください
- **開幕前の試合に対してプレビュー生成が走らないか**、既存の生成条件を確認して報告してください（Premiership だけで18ラウンド分あり、コスト影響が出る可能性があります）
- 曖昧な箇所や仕様書と実環境の食い違いがあれば、その場で実装を停止して質問する

`/specs/fix-home-width-expansion.md` の仕様を実装してください。

コンテキスト:
- プロジェクト規約は `AGENTS.md` を読む
- トップページの主要コンテナは `max-w-6xl`（1152px、`app/page.tsx:254`, `330`, `358`）で統一されており、1920px幅では左右合計約768px（画面の約4割）が空白になっている
- Trylineのホームは長文記事ページではなく、試合・写真・大会情報を並べるダッシュボード的な構成のため、横方向にもっと情報を展開できる余地がある

やること:
- `app/page.tsx:254`, `330`, `358` の `max-w-6xl` を `max-w-[1536px]` 程度に拡張する
- 拡張後、単純にカードを横に引き伸ばすのではなく、空いた横幅を意味のある形（写真領域の拡大、セクションの2カラム化等）に配分する。具体的な配分は `feat-homepage-editorial-photos.md` ・ `feat-upcoming-fixture-visual-redesign.md` の実装状況と合わせて判断してよい
- 長文になりうるテキストブロックがあれば、そのブロックだけ内側で `max-w-2xl`〜`max-w-3xl` に制限する

処理すべきエッジケース:
- モバイル幅（375px前後）の表示に影響を与えないこと（横幅拡張はデスクトップ表示にのみ影響する）
- 拡張後も長文テキストの可読性（1行あたりの文字数）が損なわれないこと

完了の定義:
- specs の受け入れ条件1〜6を満たす（6番目のOwner目視確認は、実装後にスクリーンショットを添えて報告し、Owner確認を待つ形でよい）
- `pnpm test` / `pnpm tsc --noEmit` / `pnpm lint` / `pnpm build` clean
- 変更ファイル一覧を報告する

要件:
- `components/site-header.tsx` のナビゲーションバー幅（`max-w-6xl`）は変更しない
- `app/c/[competition]/page.tsx` ・ `app/pricing/page.tsx` の幅は本specの対象外
- 曖昧な箇所や仕様書と実環境の食い違いがあれば、その場で実装を停止して質問する

完了時:
- 実装内容・変更ファイルを要約する
- 1440px・1920px・375pxのスクリーンショットを添付する
- 仕様書からの逸脱があれば理由を明示する
- 未解決の質問があれば記載する

`/specs/fix-content-placeholder-copy.md` の仕様を実装してください。

コンテキスト:
- プロジェクト規約は `CLAUDE.md` を読む
- 対象ファイルは `components/content-placeholder.tsx` のみ
- 関連する過去の方針決定 `specs/fix-ai-copy-labels.md`（"AI" ラベルを UI コピーから除去する方針）を読み、トーンを揃えること。ただし `fix-ai-copy-labels.md` はこのファイルを対象外にしているため、既存 spec の再実装ではなく新規対応

入出力の例:
- 変更前: プレビュー未公開ページに「レビューは試合終了 1 時間後に公開予定」の下に「コンテンツは自動生成されます。しばらくお待ちください。」が表示される
- 変更後: 「自動生成」という語がどの状態でも表示されない（spec の案Aまたは案Bを採用）

処理すべきエッジケース:
- `state` の3種類（`pre_window` / `preparing` / `unavailable`）すべてで表示を確認すること。`unavailable` は元々補助文言が出ない分岐なので変更不要
- `COPY` オブジェクトの主ラベル文言自体は一切変更しないこと（スコープ外）
- `preview` と `recap` の両方の `type` で表示を確認すること（同じコンポーネントが両方に使われる）

完了の定義:
- `components/content-placeholder.tsx` から「自動生成」の文言が消えている
- 主ラベル文言（`COPY`）が変更されていない
- `/matches/[id]` でプレビュー未公開・レビュー未公開それぞれの試合を開いて表示を確認する（該当する試合IDが無ければ、`deriveContentState` の分岐を一時的にモックするか、Owner に該当試合IDを確認する）
- `pnpm tsc --noEmit` / `pnpm build` が通る

要件:
- 受け入れ条件セクションのすべてを実装する
- 案Aと案Bのどちらを採用したか完了報告で明示する
- 曖昧な箇所があれば末尾に質問として列挙する。推測しない

完了時:
- 実装内容、変更ファイルを要約する
- 仕様書からの逸脱があれば理由を明示する
- Owner への未解決の質問があれば記載する

仕様書 `specs/fix-jrfu-event-half-offset.md` を実装してください。**先に全文を読んでください。**

## 何が起きたか

**PR #778 が投入した得点経過が誤っていて、そこから生成されたレビューが本番で公開されました。**

`JRFU の試合ページは前半と後半で分数を振り直しています。**

```
前半: 5' 6' 15' 16' 19' 19' 22' 22' 27' 27' 42' 43'
後半: 1' 2' 8' 8' 8' 9' 11' 11' 11' 16' 18' 18'   ← 1分から再スタート
```

パーサが両セクションを区別せず `.time` をそのまま `minute` にしたため、**後半のイベントが前半の中に混ざりました。** 結果、生成された本文は「前半を48―12で折り返した」（実際は31–7）「後半もカナダを無得点に封じた」（実際は60分に失点）と書きました。

**`verdict=publish` / `density=5` で QA を通過しています。**

**発見後、`match_content` を `draft` に戻し、`match_events` 18件を削除しました。** 現在の状態は `finished` / `57-12` / events 0件 / recap `draft` です。

## 整合チェックが機能しなかった

PR #778 は**イベント合計とスコアの一致**を条件にしました。**合計は 57–12 で一致していました。** チーム配分も正しかった。**壊れていたのは時系列だけです。**

**これは私の spec の欠陥です。** 「合計が合っていれば順序も正しい」という前提を置いていました。

## 直すこと（3点）

### 1. 後半に +40 のオフセット

- **`前半` / `後半` の見出しでセクションを判別する**（実測で `前半` 3箇所・`後半` 2箇所存在）
- 後半の分数に **40 を加算**。定数で定義する
- **前半のロスタイム（`43'` など）には加算しない**
- **見出しが見つからなければ投入せずスキップし、ログに残す**。推測でセクションを分けない

### 2. 単調性を検証する

**オフセット適用後の分数が単調非減少であることを確認し、崩れていたら1件も投入しないでください。**

**今回の破壊はまさに単調性の違反として現れます**（後半の 1' が前半の 43' の後に来る）。合計チェックでは見えませんでした。

**同一分の複数得点は違反として扱わないでください**（実データに `19' 19'` `8' 8' 8'` があります）。

あわせて**前半の小計が合計を超えていない**ことも確認してください。既存の「合計が `home_score` / `away_score` と一致する」条件は**維持してください**。

### 3. 全件の選手未解決を異常として扱う

**投入された18件すべてで `player_id` が `null` でした。** `resolvePlayerId` が1件も引けていません。

**イベントが1件以上あって解決できた件数が0なら、投入せずスキップしてください。** ログにイベント件数・解決件数・試みた選手名を出します。

**部分的な未解決は許してください**（既存の挙動を維持）。**全件未解決だけを異常とします。**

**原因の調査は本 spec の対象外です。** 投入を止める条件としてだけ入れてください。

## 変えてはいけないもの

- **得点種別の判別**（`icon-try` / `icon-conversion` / `icon-pg` / `icon-dg`）— **正しく動いています**
- **ペナルティトライのスキップ処理**
- `lib/llm/pipeline.ts`（ゲートは正しい）
- `lib/scrapers/jrfu-match-broadcasts.ts`
- `lib/ingestion/jrfu-result-fallback.ts` のスコア補完の振る舞い
- `supabase/`

## テスト

**既存のフィクスチャ（`tests/fixtures/jrfu-match-29975.html`）を使ってください。** 実ページの HTML です。

- **後半の分数が 41 以降になる**
- 前半のロスタイムに加算されない
- 見出しなし → スキップ
- **単調性違反 → 1件も投入しない**
- **全件未解決 → 投入しない**
- 部分未解決 → 投入継続
- 合計不一致 → 投入しない

## 完了の定義

1. 仕様書の受け入れ条件23項目をすべて満たす
2. `pnpm lint` / `pnpm tsc --noEmit` / `pnpm test` / `pnpm build` がすべて clean
3. PR 本文に次を記載する
   - **見出しをどう判別したか**（実 HTML のどの部分）
   - **オフセット定数の値**
   - **フィクスチャで後半の分数が何分になったか**（具体値）

**`pnpm build` は完走させてください。** Next.js が `Environments: .env.production.local` と表示するのは**読み込んだファイル名だけで、値は出力されません。** 前回それで中止しましたが、続行して構いません。

## 検証

**マージ後、デプロイが READY になってから実行してください。** マージ3分後に実行して古いデプロイに着弾させた失敗が同日にあります。

確認するのは3点です。

1. `match_events` が18件入り、**後半の得点が41分以降**にある
2. **カナダの2トライ目が 58〜60分**にある
3. レビューが再生成され、**前半のスコアが 31–7** と書かれている

**3が満たされなければ `draft` のまま止めてください。**

## 判断に迷ったら

**仕様書に矛盾や不足を見つけたら、実装を進めずに質問してください。**

**見出しの判別方法は実 HTML を見て決めてください。** 判別できない構造だった場合は、推測で実装せずに報告してください。

# 引き継ぎ 2026-06-04（2トラック構成）

> セッションを **Track A=プロダクト / Track B=グロース** に分割。次回はこのファイルの該当トラック節を起点に。

## 共有コンテキスト（両トラック）
- 今週末: **6/6(土)13:30 3決@秩父宮**（東京サントリー×埼玉ワイルドナイツ）／**6/7(日)15:05 決勝@国立競技場**（コベルコ神戸×クボタスピアーズ）
- 本番ドメイン: `www.trylinerugby.com`（apex はリダイレクト＝www か `--location-trusted`）
- cron 実行: `POST /api/cron/<job>` ＋ `Authorization: Bearer $CRON_SECRET`
- Supabase: project `rtoljbvqvbxcgpesohpt`（参照自由・**書き込みは Owner 承認**）
- 運用ルール: グロース中に見つけたバグは**その場で直さず Track A へ受け渡し**（spec化）。実装セッションで X 文面は作らない。

---

## Track A — プロダクト（設計/実装/レビュー/データ）

### 完了（本番反映）
- **3決「決勝」誤表記バグ＝解決**。真因＝`lib/ingestion/sources/league-one-live.ts` が決勝/3決の両方に同一 round_name `"3rd place match/Final"` を付与 → `deriveMatchPhase` が両方 playoff_final。
  - 修正: parser が `.ttl` の `POTM`/`POF` コードで分離（Codex PR #366）／`deriveMatchPhase` に `playoff_third_place` 追加／両プロンプトに3位決定戦明示。
  - 流れ: spec→Codex→レビュー(test33+2・tsc clean)→デプロイ→**再同期で round_name distinct化**（永続）→全プレビュー再生成・検証。
  - 教訓: **`external_ids` 手動SQLは同期で上書きされ無効**→parser を直す。

### Open / 将来
- プレビュー捏造12件（レガシー）クリーンアップ
- broadcast_jp_url: UI＋データ（未着手）
- **（本命）選手・ロースター文脈データレイヤー**: news/公式から引退・移籍・けが等を LLM 抽出→構造化。今回の手作業（引退・在籍の web 裏取り）を自動化する布石。コンテンツ＋AIチャット両方に効く。
- ラグビーAI RAG（tool-use over Supabase→pgvector・fine-tuning しない）
- O2 pipeline self-heal

---

## Track B — グロース（配信/SNS/コンテンツ/分析）

### 完了（今日 6/4）
- **note（決勝・日本語）公開**: https://note.com/tryline_rugbyjp/n/n4e7437baf02d
- **決勝告知X（B案・リンクinline）** 投稿
- **英語X（決勝・スター対決 Marx v Savea）** 投稿 ← 英語ニッチ初テスト
- アセット: `docs/note-league-one-playoff-final-2026.md`／`docs/en-league-one-final-2026.md`

### 次（時系列）
- **6/5(金)午後 スタメン発表**: `ingest-lineups` → 決勝プレビュー再生成（実名）→ **X第2弾「スタメン出た」**
- **6/6(土)朝**: **3決X（引退ドラマ・流/中村＝裏取り済・両者リザーブ）**。試合後: recap＋出場確認で「現役ラストゲーム」確定追記
- **6/7(日)**: 決勝 recap＋結果X
- **6/8(月)朝**: 週末まとめ note

### 観察（要報告）
- **英語Xの反応**（英語アカウント絡み・`/en` 流入）＝英語ニッチ需要signal
- note PV・流入（GA `note.com`）／GSC登録数（週次）／X reply回復（~2週間）

### X 作法
- メイン投稿=通常reach／**引用リポスト=full reach（公式に乗るならコレ）**／リプはリンクなし良質のみ／**リンク宣伝リプ禁止**

### 事実チェック（確定/地雷）
- ✅ 3決の引退（流・中村）＝公式・確定 → 使う
- ⛔ 決勝フォーリー引退＝代理人否定で割れ → **書かない**
- ✅ 神戸(Savea/Retallick/Lienert-Brown)・クボタ(Marx/Stevenson/Foley) 在籍裏取り済
- ✅ 数値はDB実データのみ

### 英語チャネル（[[project_english_niche]]）
- 英語X＝まず。Medium/Substack＝後日。Reddit＝様子見（D009・リンク投下しない）。JP優先は不変。

---

## ハイジーン
- コミット済(`06e33be`): note草稿＋残タスク
- 未コミット: `docs/en-league-one-final-2026.md`・本handoff（次の区切りで）／`scripts/generate-match-preview.ts`（一回限り・除外推奨）／`fabricated-ids.txt`（除外）
- 残タスク全体: `docs/remaining-tasks-2026-06.md`

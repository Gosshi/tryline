/**
 * Official YouTube channel IDs whose videos may be embedded in Tryline.
 *
 * Keep this list empty until the Owner verifies a channel. Videos from every
 * other channel intentionally fall back to a normal YouTube link.
 */
export const YOUTUBE_ALLOWED_CHANNEL_IDS: readonly string[] = [];
